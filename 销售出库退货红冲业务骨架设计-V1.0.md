# 销售出库退货红冲业务骨架设计 V1.5

> 版本：V1.5（第 21.5 步 chief 范围裁决修订版）  
> 日期：2026-05-02  
> 创建人：backend (DeepSeek-V4-Pro)  
> 审计人：Codex（第 21-R3~R5 共五轮复审通过）  
> 裁决人：chief（第 21.5 步 14 项范围裁决确认）  
> 冻结人：chief  
> 本步只做文档设计，不改业务代码  
> **V1.5 修订范围：**  
> - 第 5 项 chief 裁决：SN 强制绑定出库，移除无 SN 商品出库分支  
> - 第 6 项 chief 裁决：价格修改权统一归 sale:manage，不新增 price:edit  
> - 第 8 项 chief 裁决：不在销售模块乱预留 creditLimit 字段  
> - 第 1~4, 7, 9~14 项裁决确认原建议，无变更  
> - 14 项裁决结果正式写入第 10.1 节  

---

## 文档说明

本文档为第 21 步输出，对销售出库、退货、红冲业务的**完整骨架进行前置设计**。

**参考基线：**
- [SN-ERP-启动方案-V1.2-实施合并版.md](file:///Users/zh/Documents/trae_projects/TSN/docs/SN-ERP-启动方案-V1.2-实施合并版.md)
- [SN-ERP-UI设计基线-V1.1-补强版.md](file:///Users/zh/Documents/trae_projects/TSN/docs/SN-ERP-UI设计基线-V1.1-补强版.md)
- [操作日志动作字典与覆盖复查-V1.0.md](file:///Users/zh/Documents/trae_projects/TSN/docs/操作日志动作字典与覆盖复查-V1.0.md)
- [SN-ERP-业务规则说明书-V6-冻结基线.md](file:///Users/zh/Documents/trae_projects/TSN/docs/SN-ERP-业务规则说明书-V6-冻结基线.md)
- 当前已冻结第 1-20-C 步实现与文档

**本步红线：不修改任何业务代码、schema、migration、seed。**

---

## 一、业务范围

### 1.1 核心业务流程

| 序号 | 业务 | 说明 |
|------|------|------|
| 1 | 销售单 | 向经销商销售商品的单据，草稿 + 确认 + 出库阶段 |
| 2 | 销售出库 | 创建出库单（DRAFT）后确认出库单才扣减库存、扣减 SN、生成出库记录 |
| 3 | 销售退货 | 经销商退回已出库商品，支持 SN 退回和非 SN 退回 |
| 4 | 红冲 | 对销售出库或退货进行反向冲销，生成红冲记录 |
| 5 | 红冲回退 | 对已红冲的记录进行回退，恢复原状态 |

### 1.2 与现有模块的边界关系

| 关系 | 说明 |
|------|------|
| **与采购入库** | 采购入库增加库存，销售出库减少库存。库存不能为负数（除非业务明确允许）。 |
| **与 SN** | 出库时扣减 SN（状态从 IN_STOCK → OUTBOUND），退货时恢复 SN（OUTBOUND → IN_STOCK），红冲时回退 SN 状态（OUTBOUND → IN_STOCK）。SN 全局唯一规则不可破坏。 |
| **与库存** | 出库减少 availableQuantity 和 totalQuantity，退货增加 availableQuantity 和 totalQuantity。 |
| **与经销商** | 销售单必须绑定经销商。经销商快照保留在销售单中。 |
| **与操作日志** | 所有写操作必须记录操作日志（参考第 20-C 步动作字典）。 |

---

## 二、核心规则

### 2.1 销售单规则

1. 销售单保存 = **草稿**，不直接影响库存。
2. 销售单必须绑定一个经销商（dealerId）。
3. 一个销售单可包含多行销售明细（sale_order_item）。
4. 每行明细绑定一个商品（productId），包含销售数量、销售单价、销售金额。
5. 销售单号自动生成，格式建议：`XS + YYYYMMDD + NNNN`。
6. 销售单创建时生成经销商快照和商品快照。
7. 草稿状态可编辑、可取消。
8. **一个销售单允许多次部分出库**。每次出库创建一张出库单（biz_sale_outbound），出库数量累加到 outboundTotalQuantity。
9. 出库完成（outboundTotalQuantity = totalQuantity）后，销售单状态变为 OUTBOUND_COMPLETED，不可继续出库。
10. **CONFIRMED** 销售单：不可编辑、不可删除、可以取消（→ CANCELLED）、不允许红冲。
11. **PARTIAL_OUTBOUND** 销售单：不可编辑、不可删除、不可普通取消、只能走退货或红冲链路。
12. **OUTBOUND_COMPLETED** 销售单：不可编辑、不可删除、不可普通取消、只能走退货或红冲链路。
13. 历史快照不跟随主数据变更。

### 2.2 出库规则

1. 创建出库单（DRAFT）不影响库存、不改变 SN 状态。
2. 确认出库单（DRAFT → CONFIRMED）后才影响库存。
3. DRAFT 出库单可编辑、可取消。CONFIRMED 出库单不可编辑、不可取消，只能走退货或红冲链路。
4. 出库必须占用 / 扣减 `availableQuantity`（可出库数量）。
5. 出库涉及 SN 时，确认出库单后 SN 状态从 `IN_STOCK` 变为 `OUTBOUND`。
6. 已出库的 SN 不可重复出库（通过 biz_sale_outbound_sn 校验占用）。
7. 库存不足时确认出库单应拒绝，返回当前可用库存数量。
8. 出库确认必须二次确认（前端 + 后端权限双重校验）。
9. 出库操作必须写库存流水和 SN 时间线。

### 2.3 退货规则

1. 退货必须绑定原出库记录（或原销售单 + 商品）。
2. 退货单保存 = 草稿，确认后才影响库存。
3. 退货确认后，库存数量恢复（totalQuantity 和 availableQuantity 增加）。
4. 退货涉及 SN 时，SN 状态从 `OUTBOUND` 恢复为 `IN_STOCK`（验收通过直接可售）。
5. 退货必须保留退货原因。
6. 退货数量不可超过原出库数量。

### 2.4 红冲规则

1. 红冲必须绑定原业务单据（销售出库单或退货单）。
2. 红冲确认后生成反向流水。
3. 红冲涉及金额时，金额必须反向（正变负 / 负变正）。
4. 红冲涉及 SN 时，SN 状态必须回退。
5. 红冲必须保留原因、时间线和操作人。
6. 红冲不可撤销（只能通过再次红冲回退）。
7. 不允许无痕删除业务记录。

### 2.5 SN 相关规则（chief 第 5 项裁决：强制绑定 SN）

> **裁决结论（第 21.5 步）：所有销售出库必须绑定 SN。V1 不支持无 SN 商品直接按数量出库。**

1. SN 必须全局唯一。
2. SN 状态闭环：IN_STOCK → OUTBOUND → IN_STOCK（退货后恢复）。
3. **所有销售出库必须绑定具体 SN。**
4. 出库确认时校验以下条件（必须全部通过）：
   - SN 存在且状态 = IN_STOCK。
   - SN 对应 productId 与销售明细 productId 一致。
   - SN 未被其他 CONFIRMED 出库单占用 / 出库。
   - 每一条出库明细的 quantity 必须等于已绑定 SN 数量（outboundSnCount）。
   - 未绑定 SN、SN 数量不足、SN 校验不通过，均**拒绝确认出库**。
   - **不要求** SN 的采购明细、采购批次必须属于本销售单。
5. SN 的采购明细、采购批次、导入批次仅作为追溯来源，不构成出库校验前提。
6. 所有出库 SN 追溯统一记录在 `biz_sale_outbound_sn` 中（purchaseDetailId / importBatchId）。
7. 退货确认后，退回 SN 状态从 OUTBOUND 恢复为 IN_STOCK。
8. 第一阶段不限制同一销售明细只能选择单一采购批次的 SN（第 12 项裁决确认）。
9. V1 不设计无 SN 商品出库分支。

### 2.6 金额规则

1. 金额字段使用 Decimal，避免浮点误差（参考 biz_purchase_detail.unitPrice / totalPrice 的 `@db.Decimal(15, 2)`）。
2. 销售单价和销售金额必须保留在销售明细快照中。
3. 退货金额必须等于原出库金额（不允许退货改价，除非有红冲流程）。
4. 红冲金额必须等于原单据金额的负数。
5. 导出模板中允许展示非促销价总金额、促销价总金额、合计总金额。

---

## 三、建议表结构草案

### 3.1 命名规范

沿用现有 Prisma 命名规范：
- 表名：`biz_` 前缀，snake_case
- 主键：`id`，UUID，`@default(dbgenerated("gen_random_uuid()"))`
- 外键：`xxx_id`，snake_case
- 时间：`created_at` / `updated_at` / `confirmed_at`，Timestamptz
- 金额：`@db.Decimal(15, 2)`
- 快照：`Json` 类型
- 状态：`@db.VarChar(20)`

---

### 3.2 biz_sale_order（销售单）

| 字段 | 含义 | 类型 | 必填 | 关联 | 备注 |
|------|------|------|:--:|------|------|
| id | 主键 | Uuid | ✅ | — | gen_random_uuid() |
| orderNo | 销售单号 | VarChar(20) | ✅ | UNIQUE | XS+YYYYMMDD+NNNN |
| dealerId | 经销商ID | Uuid | ✅ | → biz_dealer.id | 绑定经销商 |
| dealerSnapshot | 经销商快照 | Json | ✅ | — | 创建时快照 |
| status | 状态 | VarChar(20) | ✅ | — | DRAFT/CONFIRMED/PARTIAL_OUTBOUND/OUTBOUND_COMPLETED/CANCELLED/RED_REVERSED |
| saleDate | 销售日期 | Date | ✅ | — | |
| totalAmount | 销售总额 | Decimal(15,2) | ✅ | — | 明细 totalPrice 合计 |
| totalQuantity | 销售总数量 | Int | ✅ | — | 明细 quantity 合计 |
| outboundTotalQuantity | 累计出库数量 | Int | ✅ | — | default: 0，每次出库后累加 |
| remark | 备注 | Text | ❌ | — | |
| isRedReversed | 是否已红冲 | Boolean | ✅ | — | default: false |
| confirmedAt | 销售单确认时间 | Timestamptz | ❌ | — | 首次 confirm 时写入 |
| confirmedBy | 确认人 | Uuid | ❌ | → sys_user.id | |
| completedAt | 出库完成时间 | Timestamptz | ❌ | — | outboundTotalQuantity = totalQuantity 时 |
| createdAt | 创建时间 | Timestamptz | ✅ | — | now() |
| updatedAt | 更新时间 | Timestamptz | ✅ | — | @updatedAt |
| createdBy | 创建人 | Uuid | ✅ | → sys_user.id | |
| updatedBy | 更新人 | Uuid | ❌ | → sys_user.id | |

索引：
- `@@index([status])`
- `@@index([dealerId])`
- `@@index([saleDate(sort: Desc)])`
- `@@index([createdBy])`

---

### 3.3 biz_sale_order_item（销售单明细）

| 字段 | 含义 | 类型 | 必填 | 关联 | 备注 |
|------|------|------|:--:|------|------|
| id | 主键 | Uuid | ✅ | — | |
| saleOrderId | 销售单ID | Uuid | ✅ | → biz_sale_order.id | |
| lineNumber | 行号 | Int | ✅ | — | 从 1 递增 |
| productId | 商品ID | Uuid | ✅ | → biz_product.id | |
| productSnapshot | 商品快照 | Json | ✅ | — | 创建时快照 |
| quantity | 销售数量 | Int | ✅ | — | > 0 |
| unitPrice | 销售单价 | Decimal(15,2) | ✅ | — | |
| totalPrice | 销售金额 | Decimal(15,2) | ✅ | — | quantity × unitPrice |
| priceType | 价格类型 | VarChar(30) | ❌ | — | 原价/特价/一口价 |
| snCount | 已录入SN数量 | Int | ✅ | — | default: 0 |
| outboundQuantity | 已出库数量 | Int | ✅ | — | default: 0 |
| returnedQuantity | 已退货数量 | Int | ✅ | — | default: 0 |
| remark | 备注 | VarChar(500) | ❌ | — | |

索引：
- `@@unique([saleOrderId, lineNumber])`
- `@@index([saleOrderId])`
- `@@index([productId])`

---

### 3.4 biz_sale_outbound（销售出库单）

| 字段 | 含义 | 类型 | 必填 | 关联 | 备注 |
|------|------|------|:--:|------|------|
| id | 主键 | Uuid | ✅ | — | |
| outboundNo | 出库单号 | VarChar(20) | ✅ | UNIQUE | CK+YYYYMMDD+NNNN |
| saleOrderId | 销售单ID | Uuid | ✅ | → biz_sale_order.id | 一个销售单可有多个出库单 |
| status | 状态 | VarChar(20) | ✅ | — | DRAFT/CONFIRMED/CANCELLED/RED_REVERSED |
| totalQuantity | 出库总数量 | Int | ✅ | — | |
| totalAmount | 出库总金额 | Decimal(15,2) | ✅ | — | |
| isRedReversed | 是否已红冲 | Boolean | ✅ | — | default: false |
| confirmedAt | 确认时间 | Timestamptz | ❌ | — | DRAFT 时为空，CONFIRMED 时必填 |
| confirmedBy | 确认人 | Uuid | ❌ | → sys_user.id | DRAFT 时为空，CONFIRMED 时必填 |
| cancelledAt | 取消时间 | Timestamptz | ❌ | — | CANCELLED 时必填 |
| cancelledBy | 取消人 | Uuid | ❌ | → sys_user.id | CANCELLED 时必填 |
| cancelReason | 取消原因 | VarChar(500) | ❌ | — | 取消时必填（前端强提示） |
| createdAt | 创建时间 | Timestamptz | ✅ | — | |
| createdBy | 创建人 | Uuid | ✅ | → sys_user.id | |
| updatedAt | 更新时间 | Timestamptz | ✅ | — | |

索引：
- `@@index([saleOrderId])`
- `@@index([status])`

---

### 3.5 biz_sale_outbound_item（出库明细）

| 字段 | 含义 | 类型 | 必填 | 关联 | 备注 |
|------|------|------|:--:|------|------|
| id | 主键 | Uuid | ✅ | — | |
| outboundId | 出库单ID | Uuid | ✅ | → biz_sale_outbound.id | |
| saleOrderItemId | 销售明细ID | Uuid | ✅ | → biz_sale_order_item.id | |
| quantity | 本次出库数量 | Int | ✅ | — | |
| outboundSnCount | 出库SN数量 | Int | ✅ | — | default: 0，SN 追溯以 biz_sale_outbound_sn 为准 |

索引：
- `@@index([outboundId])`
- `@@index([saleOrderItemId])`

---

### 3.6 biz_sale_outbound_sn（出库 SN 明细关联表，新增）

> **用途：** 记录每一个出库 SN 的明细追溯关系，支持 SN 粒度精确追踪和冲突检测。

| 字段 | 含义 | 类型 | 必填 | 关联 | 备注 |
|------|------|------|:--:|------|------|
| id | 主键 | Uuid | ✅ | — | |
| outboundId | 出库单ID | Uuid | ✅ | → biz_sale_outbound.id | |
| outboundItemId | 出库明细ID | Uuid | ✅ | → biz_sale_outbound_item.id | |
| saleOrderId | 销售单ID | Uuid | ✅ | → biz_sale_order.id | 冗余便于查询 |
| saleOrderItemId | 销售明细ID | Uuid | ✅ | → biz_sale_order_item.id | 冗余便于查询 |
| productId | 商品ID | Uuid | ✅ | → biz_product.id | 冗余便于查询 |
| snId | SN ID | Uuid | ✅ | → biz_sn.id | 出库 SN 的 biz_sn 主键 |
| snCode | SN 编码 | VarChar(100) | ✅ | — | 快照，冗余便于查询 |
| purchaseDetailId | 采购明细ID | Uuid | ❌ | → biz_purchase_detail.id | 追溯 SN 的采购来源 |
| importBatchId | 导入批次ID | Uuid | ❌ | → biz_sn_import_batch.id | 追溯 SN 的导入批次 |
| statusSnapshot | 出库前状态 | VarChar(20) | ✅ | — | 出库前 SN 状态快照（通常为 IN_STOCK） |
| createdAt | 创建时间 | Timestamptz | ✅ | — | now() |

索引：
- `@@unique([outboundId, snId])` — 同一出库单同一 SN 不可重复
- `@@index([snId])` — 按 SN 追溯出库记录
- `@@index([outboundId])`
- `@@index([outboundItemId])`
- `@@index([purchaseDetailId])`

**约束说明：**
- 创建 DRAFT 出库单时可预录入 SN 关联（写入 biz_sale_outbound_sn），但不校验库存占用。
- 确认 CONFIRMED 出库单时校验：该 SN 是否已被**其他 CONFIRMED 出库单**占用。
- SN 商品出库必须写 biz_sale_outbound_sn；无 SN 商品不需要。
- 一个出库明细可关联多个 SN（来自不同采购明细 / 批次）。
- 后续退货、红冲可按此表精确回退（按 SN 恢复为 IN_STOCK）。

### 3.7 biz_sale_return（退货单）

| 字段 | 含义 | 类型 | 必填 | 关联 | 备注 |
|------|------|------|:--:|------|------|
| id | 主键 | Uuid | ✅ | — | |
| returnNo | 退货单号 | VarChar(20) | ✅ | UNIQUE | TH+YYYYMMDD+NNNN |
| outboundId | 出库单ID | Uuid | ✅ | → biz_sale_outbound.id | 绑定原出库 |
| saleOrderId | 销售单ID | Uuid | ✅ | → biz_sale_order.id | 冗余，方便查询 |
| dealerId | 经销商ID | Uuid | ✅ | → biz_dealer.id | |
| status | 状态 | VarChar(20) | ✅ | — | DRAFT/CONFIRMED/RED_REVERSED |
| totalQuantity | 退货总数量 | Int | ✅ | — | |
| totalAmount | 退货总金额 | Decimal(15,2) | ✅ | — | |
| returnReason | 退货原因 | Text | ✅ | — | |
| isRedReversed | 是否已红冲 | Boolean | ✅ | — | default: false |
| confirmedAt | 确认时间 | Timestamptz | ❌ | — | |
| confirmedBy | 确认人 | Uuid | ❌ | → sys_user.id | |
| createdAt | 创建时间 | Timestamptz | ✅ | — | |
| createdBy | 创建人 | Uuid | ✅ | → sys_user.id | |
| updatedAt | 更新时间 | Timestamptz | ✅ | — | |

索引：
- `@@index([outboundId])`
- `@@index([saleOrderId])`
- `@@index([status])`

---

### 3.8 biz_sale_return_item（退货明细）

| 字段 | 含义 | 类型 | 必填 | 关联 | 备注 |
|------|------|------|:--:|------|------|
| id | 主键 | Uuid | ✅ | — | |
| returnId | 退货单ID | Uuid | ✅ | → biz_sale_return.id | |
| outboundItemId | 出库明细ID | Uuid | ✅ | → biz_sale_outbound_item.id | |
| saleOrderItemId | 销售明细ID | Uuid | ✅ | → biz_sale_order_item.id | |
| productId | 商品ID | Uuid | ✅ | → biz_product.id | |
| productSnapshot | 商品快照 | Json | ✅ | — | 退货时快照 |
| quantity | 退货数量 | Int | ✅ | — | ≤ 出库数量 |
| unitPrice | 退货单价 | Decimal(15,2) | ✅ | — | 等于原出库单价 |
| totalPrice | 退货金额 | Decimal(15,2) | ✅ | — | |
| snCodes | 退货SN列表 | Json | ❌ | — | |
| remark | 备注 | VarChar(500) | ❌ | — | |

---

### 3.9 biz_red_reverse（统一红冲表）

| 字段 | 含义 | 类型 | 必填 | 关联 | 备注 |
|------|------|------|:--:|------|------|
| id | 主键 | Uuid | ✅ | — | |
| reverseNo | 红冲单号 | VarChar(20) | ✅ | UNIQUE | HC+YYYYMMDD+NNNN |
| sourceType | 源单据类型 | VarChar(20) | ✅ | — | sale_outbound / sale_return / purchase_order |
| sourceId | 源单据ID | Uuid | ✅ | — | |
| sourceOrderNo | 源单据号 | VarChar(50) | ✅ | — | 快照 |
| status | 状态 | VarChar(20) | ✅ | — | DRAFT/CONFIRMED |
| totalQuantity | 红冲数量 | Int | ✅ | — | |
| totalAmount | 红冲金额 | Decimal(15,2) | ✅ | — | 负数 |
| reverseReason | 红冲原因 | Text | ✅ | — | |
| confirmedAt | 确认时间 | Timestamptz | ❌ | — | |
| confirmedBy | 确认人 | Uuid | ❌ | → sys_user.id | |
| createdAt | 创建时间 | Timestamptz | ✅ | — | |
| createdBy | 创建人 | Uuid | ✅ | → sys_user.id | |
| updatedAt | 更新时间 | Timestamptz | ✅ | — | |

索引：
- `@@index([sourceType, sourceId])`
- `@@index([status])`

---

### 3.10 biz_red_reverse_item（红冲明细）

| 字段 | 含义 | 类型 | 必填 | 关联 | 备注 |
|------|------|------|:--:|------|------|
| id | 主键 | Uuid | ✅ | — | |
| reverseId | 红冲单ID | Uuid | ✅ | → biz_red_reverse.id | |
| sourceItemId | 源明细ID | Uuid | ✅ | — | 源单据明细行ID |
| productId | 商品ID | Uuid | ✅ | → biz_product.id | |
| productSnapshot | 商品快照 | Json | ✅ | — | |
| quantity | 红冲数量 | Int | ✅ | — | 负数 |
| unitPrice | 红冲单价 | Decimal(15,2) | ✅ | — | |
| totalPrice | 红冲金额 | Decimal(15,2) | ✅ | — | 负数 |
| snCodes | 红冲SN | Json | ❌ | — | |

---

### 3.11 SN 状态扩展

现有 `biz_sn` 表的 status 字段已支持状态流转。出库/退货/红冲需要新增 SN 状态值：

| 状态 | 含义 | 说明 |
|------|------|------|
| IN_STOCK | 在库（现有） | 可出库 |
| OUTBOUND | 已出库（新增） | 不可出库，不可重复出库 |
| SCRAPPED | 已报废（预留） | 不可出库 |

**RETURNED 不作为第一阶段可出库 SN 当前状态。** 退货确认后，SN 当前状态恢复为 `IN_STOCK`（商品验收通过并直接可售）。

`RETURNED` 只作为：
- 退货历史事件记录（操作日志 `sale_return_confirm`）
- SN 流转历史的 `operation_log` detail 中的事件类型
- 后续如需增加退货质检流程，再引入 `RETURN_PENDING_INSPECTION` / `RETURN_LOCKED` 等独立状态

SN 状态变更应通过操作日志的 detail.snCodes 数组记录涉及的 SN 变化。

---

## 四、状态流转

### 4.1 销售单状态流转

```
DRAFT ──confirm_order()──▶ CONFIRMED ──confirm_outbound(累计<总量)──▶ PARTIAL_OUTBOUND
  │                           │  │                                       │
  └──cancel_order()──▶ CANCELLED  └──confirm_outbound(累计=总量)──▶ OUTBOUND_COMPLETED
                                  │
                                  ├──confirm_outbound(累计<总量)──▶ PARTIAL_OUTBOUND (重新进入)
                                  │
                                  └──confirm_outbound(累计=总量)──▶ OUTBOUND_COMPLETED
                                  
PARTIAL_OUTBOUND ──red_reverse()──▶ RED_REVERSED
OUTBOUND_COMPLETED ──red_reverse()──▶ RED_REVERSED
CONFIRMED ──cancel_order()──▶ CANCELLED
```

| 状态 | 允许编辑 | 允许创建出库单 | 允许取消 | 允许红冲 | 允许删除 |
|------|:--:|:--:|:--:|:--:|:--:|
| DRAFT | ✅ | ❌ | ✅ | ❌ | ❌（逻辑删除） |
| CONFIRMED | ❌ | ✅（admin/已授权） | ✅ | ❌ | ❌ |
| PARTIAL_OUTBOUND | ❌ | ✅（还可继续出库） | ❌ | ✅ | ❌ |
| OUTBOUND_COMPLETED | ❌ | ❌ | ❌ | ✅ | ❌ |
| CANCELLED | ❌ | ❌ | ❌ | ❌ | ❌ |
| RED_REVERSED | ❌ | ❌ | ❌ | ❌ | ❌ |

**说明：**
- `confirm_order()` = 销售单确认（PATCH /sales/orders/:id/confirm），DRAFT → CONFIRMED，不涉及库存和 SN。
- `create_outbound()` = 创建 DRAFT 出库单（POST /sales/orders/:id/outbounds），**不影响销售单状态**。
- `confirm_outbound()` = 确认出库单（PATCH /sales/outbounds/:id/confirm），根据累计出库数量自动判断销售单进入 PARTIAL_OUTBOUND 还是 OUTBOUND_COMPLETED。首次出库可以是部分出库，也可以一次性全量出库直达 OUTBOUND_COMPLETED，不要求必须经过 PARTIAL_OUTBOUND。
- 销售单从 CONFIRMED 可被取消（DRAFT / CONFIRMED → CANCELLED）。
- PARTIAL_OUTBOUND / OUTBOUND_COMPLETED 后不允许普通取消，只允许退货或红冲链路。

### 4.2 出库单状态流转

```
DRAFT ──confirm()──▶ CONFIRMED ──red_reverse()──▶ RED_REVERSED
  │
  └──cancel()──▶ CANCELLED
```

| 状态 | 允许编辑 | 允许确认 | 允许取消 | 允许红冲 |
|------|:--:|:--:|:--:|:--:|
| DRAFT | ✅ | ✅ | ✅ | ❌ |
| CONFIRMED | ❌ | ❌ | ❌ | ✅ |
| CANCELLED | ❌ | ❌ | ❌ | ❌ |
| RED_REVERSED | ❌ | ❌ | ❌ | ❌ |

出库单由销售单确认后通过 `POST /sales/orders/:id/outbounds` 创建，初始为 DRAFT，不影响库存和 SN 状态。

**确认规则：**
- `confirm()` = 出库单确认，状态 DRAFT → CONFIRMED，确认后扣减库存、改变 SN 状态、写操作日志。
- CONFIRMED 出库单不允许编辑、不允许取消，只能走退货或红冲链路。
- CANCELLED 仅允许从 DRAFT 取消。CONFIRMED 不可取消。

### 4.3 退货单状态流转

```
DRAFT ──confirm()──▶ CONFIRMED ──red_reverse()──▶ RED_REVERSED
  │                    
  └──cancel()──▶ CANCELLED
```

| 状态 | 允许编辑 | 允许确认 | 允许取消 | 允许红冲 |
|------|:--:|:--:|:--:|:--:|
| DRAFT | ✅ | ✅ | ✅ | ❌ |
| CONFIRMED | ❌ | ❌ | ❌ | ✅ |
| CANCELLED | ❌ | ❌ | ❌ | ❌ |
| RED_REVERSED | ❌ | ❌ | ❌ | ❌ |

### 4.4 红冲单状态流转

```
DRAFT ──confirm()──▶ CONFIRMED
```

| 状态 | 允许编辑 | 允许确认 | 允许取消 |
|------|:--:|:--:|:--:|
| DRAFT | ✅ | ✅ | ✅ |
| CONFIRMED | ❌ | ❌ | ❌ |

红冲一旦确认不可撤销。如需回退，创建新的红冲单冲销。

---

## 五、库存与 SN 影响口径

### 5.1 销售确认 + 出库确认

销售单 confirm（DRAFT → CONFIRMED）只标记销售单已确认，**不影响库存**。

出库单 create（POST /sales/orders/:id/outbounds）创建 DRAFT 出库单，**不影响库存、不改变 SN 状态**。

出库单 confirm（PATCH /sales/outbounds/:id/confirm，DRAFT → CONFIRMED）才影响库存：

| 影响对象 | 变化 | 说明 |
|------|------|------|
| `biz_sale_order.outboundTotalQuantity` | 增加 | 累加本次出库数量 |
| `biz_sale_order.status` | 出库确认后，若累计数量 < 总量则为 PARTIAL_OUTBOUND，等于总量则为 OUTBOUND_COMPLETED | 判断 outboundTotalQuantity 与 totalQuantity |
| `biz_sale_order_item.outboundQuantity` | 增加 | 记录每行已出库数量 |
| `biz_inventory_summary.totalQuantity` | 减少 | 减 = 出库数量 |
| `biz_inventory_summary.availableQuantity` | 减少 | 减 = 出库数量 |
| `biz_inventory_log` | 新增 | action = OUTBOUND，关联出库单ID |
| `biz_sn.status` | IN_STOCK → OUTBOUND | 仅涉及 SN 的出库，按 biz_sale_outbound_sn 逐 SN 变更 |
| `sys_operation_log` | 新增 | action = sale_outbound_confirm |

### 5.2 退货确认

| 影响对象 | 变化 | 说明 |
|------|------|------|
| `biz_inventory_summary.totalQuantity` | 增加 | 增 = 退货数量 |
| `biz_inventory_summary.availableQuantity` | 增加 | 增 = 退货数量 |
| `biz_inventory_log` | 新增 | action = RETURN_INBOUND，关联退货单ID |
| `biz_sn.status` | OUTBOUND → IN_STOCK | 退货确认后恢复可售（第一阶段默认不做质检） |
| `biz_sale_order_item.returnedQuantity` | 增加 | 记录已退货数量 |
| `sys_operation_log` | 新增 | action = sale_return_confirm |

### 5.3 红冲确认（以出库红冲为例）

| 影响对象 | 变化 | 说明 |
|------|------|------|
| `biz_inventory_summary.totalQuantity` | 增加 | 回退库存 = 原出库数量 |
| `biz_inventory_summary.availableQuantity` | 增加 | 回退库存 |
| `biz_inventory_log` | 新增 | action = RED_REVERSE，关联红冲单ID（数量为正值，表示回退） |
| `biz_sn.status` | OUTBOUND → IN_STOCK | 红冲出库时 SN 回退为在库 |
| `biz_sale_outbound.isRedReversed` | true | 标记出库单已红冲 |
| `biz_sale_order.isRedReversed` | true | 标记销售单已红冲 |
| `sys_operation_log` | 新增 | action = sale_red_reverse_confirm |

### 5.4 操作日志强制写入

以下动作必须写操作日志：

| 动作 | module | action | targetType |
|------|--------|--------|------------|
| 确认出库 | sale | sale_outbound_confirm | sale_outbound |
| 确认退货 | sale | sale_return_confirm | sale_return |
| 确认红冲 | sale | sale_red_reverse_confirm | red_reverse |
| 创建销售单 | sale | sale_order_create | sale_order |
| 编辑销售单 | sale | sale_order_update | sale_order |
| 确认销售单 | sale | sale_order_confirm | sale_order |
| 取消销售单 | sale | sale_order_cancel | sale_order |
| 创建出库单 | sale | sale_outbound_create | sale_outbound |
| 创建退货单 | sale | sale_return_create | sale_return |
| 创建红冲单 | sale | sale_red_reverse_create | red_reverse |

---

## 六、权限建议

### 6.1 新权限码清单

| 权限码 | 名称 | 模块 | 说明 |
|------|------|------|------|
| sale:manage | 销售管理 | sale | 销售单的创建、编辑、确认、取消（含 PATCH /sales/orders/:id/confirm） |
| sale:confirm | 出库确认权限 / 销售出库 | sale | 创建 DRAFT 出库单、编辑 DRAFT 出库单、确认出库单、取消 DRAFT 出库单 |
| sale:return | 销售退货 | sale | 创建和确认退货单 |
| sale:red_reverse | 销售红冲 | sale | 创建和确认红冲单 |
| sale:query | 销售查询 | sale | 查看销售数据 |
| sale:export | 销售导出 | sale | 导出销售/出库/退货数据 |

### 6.2 角色授权建议

| 权限码 | admin | operator | business | 说明 |
|------|:--:|:--:|:--:|------|
| sale:manage | ✅ | ✅ | ❌ | 商务不可创建/编辑/确认/取消销售单 |
| sale:confirm | ✅ | ❌（需授予） | ❌ | 出库确认权限，参考 purchase:confirm 机制 |
| sale:return | ✅ | ✅ | ❌ | |
| sale:red_reverse | ✅ | ❌ | ❌ | 红冲仅管理员 |
| sale:query | ✅ | ✅ | ✅ | 商务可查 |
| sale:export | ✅ | ✅ | ✅ | 复用 export:report |

### 6.3 权限规则补充

1. sale:confirm 是出库确认权限，不等于销售单确认。销售单确认归 sale:manage。
2. sale:confirm 默认仅 admin 拥有。operator 需要管理员通过 grant 接口单独授予。
3. sale:red_reverse 仅 admin 拥有，不可下放。
4. backend 必须在 controller 层校验所有权限，不可只靠前端按钮控制。
5. 确认出库必须记录操作日志（第 19 步已有 writer 机制）。

---

## 七、操作日志 action 草案

### 7.1 新增 action 清单

| 序号 | module | action | targetType | 触发接口 | 触发时机 | detail 字段 | 风险等级 |
|------|--------|--------|------------|----------|----------|-------------|----------|
| 1 | sale | sale_order_create | sale_order | POST /sales/orders | 创建成功后 | orderNo, dealerName, detailCount | 中 |
| 2 | sale | sale_order_update | sale_order | PATCH /sales/orders/:id | 编辑成功后 | orderNo, hasDetails | 中 |
| 3 | sale | sale_order_confirm | sale_order | PATCH /sales/orders/:id/confirm | 销售单确认后 | orderNo, status | 中 |
| 4 | sale | sale_order_cancel | sale_order | PATCH /sales/orders/:id/cancel | 取消成功后 | orderNo, status | 中 |
| 5 | sale | sale_outbound_create | sale_outbound | POST /sales/orders/:id/outbounds | 出库单创建后（DRAFT，不影响库存） | outboundNo, totalQuantity, outboundItemCount | 中 |
| 6 | sale | sale_outbound_confirm | sale_outbound | PATCH /sales/outbounds/:id/confirm | 出库确认后（扣减库存、改变 SN） | outboundNo, totalQuantity, totalAmount | 高 |
| 7 | sale | sale_return_create | sale_return | POST /sales/returns | 创建成功后 | returnNo, outboundId, returnReason | 中 |
| 8 | sale | sale_return_confirm | sale_return | PATCH /sales/returns/:id/confirm | 确认退货后 | returnNo, totalQuantity, totalAmount | 高 |
| 9 | sale | sale_red_reverse_create | red_reverse | POST /sales/red-reverse | 创建成功后 | reverseNo, sourceType, sourceOrderNo, reason | 高 |
| 10 | sale | sale_red_reverse_confirm | red_reverse | PATCH /sales/red-reverse/:id/confirm | 确认红冲后 | reverseNo, sourceType, totalQuantity, totalAmount | 高 |

### 7.2 detail 禁止字段

同 第 19 步 writer 规范：禁止 password / token / authorization / cookie / JWT_SECRET / DATABASE_URL 等敏感字段。

---

## 八、接口草案

### 8.1 接口清单

**销售单接口：**

| 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|
| POST | /sales/orders | sale:manage | 创建销售单（DRAFT） |
| GET | /sales/orders | sale:query | 查询销售单列表 |
| GET | /sales/orders/:id | sale:query | 查看销售单详情 |
| PATCH | /sales/orders/:id | sale:manage | 编辑销售单（仅 DRAFT） |
| PATCH | /sales/orders/:id/confirm | sale:manage | 确认销售单（DRAFT → CONFIRMED，不直接影响库存） |
| PATCH | /sales/orders/:id/cancel | sale:manage | 取消销售单 |

**出库单接口：**

| 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|
| POST | /sales/orders/:id/outbounds | sale:confirm | 创建出库单草稿（DRAFT，不扣减库存） |
| GET | /sales/outbounds | sale:query | 查询出库单列表 |
| GET | /sales/outbounds/:id | sale:query | 查看出库单详情 |
| GET | /sales/orders/:id/outbounds | sale:query | 查看该销售单的所有出库单 |
| PATCH | /sales/outbounds/:id | sale:confirm | 编辑 DRAFT 出库单 |
| PATCH | /sales/outbounds/:id/confirm | sale:confirm | 确认出库单（DRAFT → CONFIRMED，扣减库存、改变 SN 状态） |
| PATCH | /sales/outbounds/:id/cancel | sale:confirm | 取消 DRAFT 出库单 |

**退货单接口：**

| 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|
| POST | /sales/returns | sale:return | 创建退货单 |
| GET | /sales/returns | sale:query | 查询退货单列表 |
| GET | /sales/returns/:id | sale:query | 查看退货单详情 |
| PATCH | /sales/returns/:id | sale:return | 编辑退货单 |
| PATCH | /sales/returns/:id/cancel | sale:return | 取消退货单 |
| PATCH | /sales/returns/:id/confirm | sale:return | 确认退货 |

**红冲接口：**

| 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|
| POST | /sales/red-reverse | sale:red_reverse | 创建红冲单 |
| GET | /sales/red-reverse | sale:query | 查询红冲列表 |
| GET | /sales/red-reverse/:id | sale:query | 查看红冲详情 |
| PATCH | /sales/red-reverse/:id/confirm | sale:red_reverse | 确认红冲 |

### 8.2 导出接口

| 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|
| GET | /export/sale-orders | export:report | 导出销售单 |
| GET | /export/sale-outbounds | export:report | 导出出库单 |
| GET | /export/sale-returns | export:report | 导出退货单 |
| GET | /export/sale-details | export:report | 导出销售明细 |

### 8.3 关键接口参数

**POST /sales/orders**

```json
{
  "dealerId": "uuid",
  "saleDate": "2026-05-01",
  "remark": "备注",
  "details": [
    {
      "productId": "uuid",
      "quantity": 10,
      "unitPrice": 150.00,
      "priceType": "原价"
    }
  ]
}
```

**POST /sales/orders/:id/outbounds**（创建出库单草稿，DRAFT，不扣减库存）

```json
{
  "items": [
    {
      "saleOrderItemId": "uuid",
      "outboundQuantity": 5,
      "snIds": ["sn-uuid-1", "sn-uuid-2"]
    }
  ]
}
```

**PATCH /sales/outbounds/:id/confirm**（确认出库单，扣减库存、改变 SN 状态）— 无 body，后端校验后执行



**POST /sales/returns**

```json
{
  "outboundId": "uuid",
  "returnReason": "质量问题",
  "items": [
    {
      "outboundItemId": "uuid",
      "quantity": 2,
      "snCodes": ["SN-001"]
    }
  ]
}
```

**POST /sales/red-reverse**

```json
{
  "sourceType": "sale_outbound",
  "sourceId": "uuid",
  "reverseReason": "客户取消订单"
}
```

---

## 九、前端页面草案

### 9.1 销售单列表页

**路由：** `/sales/orders`

**主要字段：** 销售单号、经销商、状态、销售日期、销售总额、销售总数量、创建人、创建时间

**筛选：** keyword、dealerId、status、dateFrom、dateTo、sortBy/order

**按钮：**
- 新建销售单（sale:manage）
- 导出（export:report）
- 查看详情（点击行）

**empty/loading/error：**
- loading：Skeleton 表格
- empty：空状态插画 + "暂无销售单"
- error：Toast + 重试按钮

### 9.2 销售单详情页

**路由：** `/sales/orders/:id`

**主要区块：**
- 销售单基本信息（单号、状态、经销商、销售日期、备注）
- 销售明细表（商品编码、商品名称、数量、单价、金额、SN列表、已出库数量、已退货数量）
- 操作按钮区（根据状态和权限动态显示）

**危险操作确认：**
- 确认出库：二次确认弹窗 "确认后将扣减库存，操作不可回退"
- 取消：二次确认 "取消后不可恢复"
- 红冲：二次确认 + 原因文本输入

### 9.3 新建/编辑销售单页

**路由：** `/sales/orders/new` 或 `/sales/orders/:id/edit`

**主要字段：**
- 经销商（下拉选择，可搜索）
- 销售日期（日期选择器，默认今天）
- 备注（多行文本）
- 明细行（动态增删）：
  - 商品（下拉选择，可搜索）
  - 数量（数字输入）
  - 单价（数字输入，权限控制是否可改）
  - 价格类型（下拉：原价/特价/一口价）
  - 金额（自动计算 = 数量 × 单价）
  - SN列表（输入框，支持批量粘贴，导入 SN 校验）

**按钮：** 保存（草稿）、取消、添加明细行

### 9.4 出库单草稿与确认页

**说明：** 出库单独立于销售单。用户先在销售单详情页创建 DRAFT 出库单，然后在出库单详情页编辑并确认。

**路由：** `/sales/outbounds/:id`

**出库单详情页字段：**
- 出库单号、出库单状态（DRAFT/CONFIRMED）、关联销售单号、关联销售单详情
- 出库明细表（商品编码、名称、本次出库数量、SN列表）
- 操作按钮区（根据状态和权限动态显示）

**创建出库单（DRAFT）：**
- 在销售单详情页点击"创建出库单"。
- 选择本次出库的商品和数量（≤ 销售单未出库数量）。
- 选择出库 SN（如有 SN 商品）。
- 保存后生成 DRAFT 出库单，不影响库存和 SN 状态。

**确认出库单：**
- 在出库单详情页点击"确认出库"。
- 调用 PATCH /sales/outbounds/:id/confirm。
- 确认后才扣减库存、改变 SN 状态、写 sale_outbound_confirm 日志。
- 确认前二次校验：库存充足、SN 未被占用。
- 确认后不可编辑、不可取消。

**确认前校验：**
- 库存是否充足
- SN 是否合规（状态 = IN_STOCK）
- 权限是否具备 sale:confirm

### 9.5 退货页

**路由：** `/sales/returns` 和 `/sales/returns/new`

**新建退货：**
- 选择原出库单（搜索 + 选择）
- 自动回填出库商品明细
- 选择退货商品和数量（≤出库数量）
- 选择退货 SN（如涉及）
- 填写退货原因

**退货确认：**
- 二次确认
- 库存恢复
- SN 状态恢复

### 9.6 红冲确认页

**路由：** `/sales/red-reverse/new`

**新建红冲：**
- 选择源单据类型（出库单 / 退货单）
- 选择源单据（搜索 + 选择）
- 填写红冲原因
- 确认后生成红冲单

---

## 十、风险点与待裁决事项

### 10.1 需要 chief 裁决的核心问题

| 序号 | 问题 | 影响范围 | 建议默认值 | 优先级 |
|------|------|----------|------------|:--:|
| 1 | 是否允许部分出库 | 出库逻辑、出库明细表设计 | **是**，一个销售单可多次部分出库 | P0 |
| 2 | 销售单与出库单关系 | 表结构、接口设计 | **1:N**（本步已修正） | P0 |
| 3 | 退货是否允许部分退货 | 退货逻辑、退货明细表 | **是** | P0 |
| 4 | 红冲是否按原单全量红冲还是允许部分红冲 | 红冲表设计、库存回退逻辑 | **全量红冲**（简化第一阶段） | P0 |
| 5 | SN 是否强制绑定销售出库（即不录 SN 不许出库） | SN 校验逻辑、UI 交互 | **不强制**，允许无 SN 商品直接出库 | P1 |
| 6 | 销售价格修改权限是否由管理员控制 | 权限模型 | **是**，复用 price:view 权限，新增 price:edit | P1 |
| 7 | 退货价格是否允许不同于原出库价格 | 金额计算 | **不允许**，退货单价 = 原出库单价 | P0 |
| 8 | 经销商信用额度/预付款 | 销售单校验 | 第一阶段不做，预留字段 | P2 |
| 9 | 销售单是否需要审批流 | 状态流转 | 第一阶段不做 | P2 |
| 10 | SN 人工修正是否纳入本阶段 | 功能边界 | **不纳入**，独立步骤 | P1 |
| 11 | 红冲后是否允许重新出库 | 业务流程 | **不允许**，需创建新销售单 | P0 |
| 12 | 是否限制同一销售明细只能选择单一采购批次的 SN | SN 选择逻辑 | **第一阶段不做**（本步已修正为不限制） | P2 |
| 13 | 退货后是否增加质检流程 | 退货状态、SN 状态 | **第一阶段不做**，退货确认即恢复可售 | P2 |
| 14 | 后续是否增加 RETURN_PENDING_INSPECTION / RETURN_LOCKED 等不可售退货状态 | SN 状态流转 | **预留，后续裁决** | P2 |

### 10.2 技术风险

| 风险 | 说明 | 缓解措施 |
|------|------|----------|
| 库存并发扣减 | 高并发出库可能导致超卖 | 使用数据库行级锁 / SELECT FOR UPDATE，或乐观锁 |
| SN 状态并发变更 | 两次出库同时执行，可能重复扣减同一 SN | SN 状态变更必须在事务内，UNIQUE 约束 + 乐观锁 |
| 红冲金额精度 | Decimal 相加减可能有精度问题 | 使用 Decimal 类型，禁止浮点数 |
| 历史快照一致性 | 出库后商品被修改，历史快照需要保留 | 出库时写入 productSnapshot |

---

## 十一、验收标准

### 11.1 功能验收（15 条）

1. 销售单可以创建，初始状态为 DRAFT。
2. 草稿销售单可以编辑、取消。
3. 销售单确认后状态变为 CONFIRMED，允许创建出库单。确认销售单本身不影响库存。
4. 出库涉及 SN 时，SN 状态从 IN_STOCK 变为 OUTBOUND。
5. 库存不足时创建出库单应返回 400 + 当前可用库存。
6. 已出库 SN 不可重复出库。
7. 退货单必须绑定原出库单。
8. 退货确认后库存恢复，SN 状态恢复为 IN_STOCK（可售）。
9. 退货数量不可超过原出库数量。
10. 红冲单必须绑定原业务单据。
11. 红冲确认后生成反向库存流水。
12. 所有写操作必须记录操作日志到 sys_operation_log。
13. 所有 :id 路径参数必须有 ParseUUIDPipe。
14. 后端权限校验不可只靠前端按钮。
15. 导出接口返回 xlsx，openpyxl 可读。

### 11.2 数据一致性验收（6 条）

16. 出库前后库存汇总一致（totalQuantity = 采购入库 - 销售出库 + 退货 - 红冲）。确认销售单不影响库存，出库单确认才扣减库存。
17. SN 全局唯一约束不破坏。
18. SN 出库校验：SN 存在、IN_STOCK、productId 一致、未被占用。不要求 SN 的采购批次属于本销售单。
19. 历史快照保留，不随主数据变更。
20. 库存流水完整可追溯（每条流水关联来源单据ID）。
21. 红冲后原单据状态正确标记。

### 11.3 权限验收（4 条）

21. admin 可执行所有销售操作。
22. operator 默认不可确认出库（需授予 sale:confirm）。
23. business 不可创建/编辑/确认/取消销售单。
24. 红冲仅 admin 可执行。

### 11.4 操作日志验收（3 条）

25. 销售单创建/编辑/取消均有对应日志。
26. 出库确认/退货确认/红冲确认均有对应日志。
27. 日志 detail 不包含 password/token/authorization/cookie。

### 11.5 导出验收（2 条）

28. 销售/出库/退货均可导出 Excel。
29. 导出文件 openpyxl 可读，表头固定。

---

*文档结束。下一步需 chief 裁决 14 个待裁决问题后，方可进入第 22 步 schema + migration 实现。*

*V1.4 已根据 Codex 第四轮复审意见修正：补全 CONFIRMED 首次全量出库直达 OUTBOUND_COMPLETED 路径、前端出库确认页改为出库单草稿与确认页、业务范围"确认销售单后扣减库存"改为"确认出库单后扣减库存"。*

*建议交 Codex 复审。*
