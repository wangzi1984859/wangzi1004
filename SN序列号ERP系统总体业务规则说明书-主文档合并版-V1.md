# SN序列号ERP系统总体业务规则说明书（定稿主文档）

## 文档说明

本文件为 11 个模块文档的合并主文档。
整理原则如下：

- 保留原模块边界与原有章节结构
- 统一汇总为单一主文档，便于后续持续维护
- 当前版本仅做合并整理，不主动改写业务规则内容
- 后续如需继续优化，可在本主文档基础上补充“全局规则”“状态字典”“跨模块联动规则”等章节

## 目录

1. 商品管理模块
2. 基础资料配置模块
3. 供应商管理模块
4. 采购入库模块
5. 库存查询模块
6. 商品出库模块
7. 序列号上报模块
8. SN 全局查询模块
9. 全局进销查询模块
10. 红冲作废管理模块
11. 权限账号管理模块

---

# 第1章 商品管理模块

#### 商品管理模块定稿版

#### 一、模块定位
商品管理模块用于维护系统中的商品基础资料，整个模块以**表格型操作**为主。  
目标是让用户在一个列表页面中完成查询、筛选、排序、批量处理和基础维护，减少频繁进入详情页操作。

---

#### 二、商品字段
商品管理当前字段定义如下：

- 商品编码（必填）
- 商品名称（必填）
- 颜色（必填）
- 品类（选填）
- 品牌（选填）
- 自定义属性1（选填）
- 自定义属性2（选填）
- 自定义属性3（选填）
- 状态（启用 / 停用）

---

#### 三、新增商品规则
新增商品时：

必填字段为：
- 商品编码
- 商品名称
- 颜色

可选字段为：
- 品类
- 品牌
- 自定义属性1
- 自定义属性2
- 自定义属性3

其他规则：
- 商品编码必须唯一
- 商品编码支持手工录入
- 新建商品时可直接填写或选择品类、品牌和自定义属性
- 商品默认状态建议为“启用”

---

#### 四、编辑商品规则
编辑商品时，允许修改以下内容：

- 商品名称
- 颜色
- 品类
- 品牌
- 自定义属性1
- 自定义属性2
- 自定义属性3
- 状态

建议规则：
- 商品编码默认不允许随意修改
- 如果商品已经被后续业务引用，建议不允许删除，只允许停用

---

#### 五、商品列表页
商品管理采用表格列表展示。

支持每页显示数量：
- 20条
- 50条
- 60条

支持分页查看。

#### 列表字段
建议展示：

- 多选框
- 商品编码
- 商品名称
- 颜色
- 品类
- 品牌
- 自定义属性1
- 自定义属性2
- 自定义属性3
- 状态
- 操作

操作项包括：
- 编辑
- 删除
- 启用
- 停用

---

#### 六、筛选规则
商品列表页顶部支持筛选。

支持筛选字段：

- 商品编码
- 商品名称
- 颜色
- 品类
- 品牌
- 自定义属性1
- 自定义属性2
- 自定义属性3
- 状态

筛选方式建议：

- 商品编码：输入搜索
- 商品名称：输入搜索
- 颜色：输入搜索
- 品类：下拉选择
- 品牌：下拉选择
- 自定义属性1：输入或下拉
- 自定义属性2：输入或下拉
- 自定义属性3：输入或下拉
- 状态：下拉选择

---

#### 七、排序规则
商品列表页支持按以下字段排序：

- 商品编码
- 商品名称
- 颜色
- 品类
- 品牌
- 状态

默认排序建议：
- 按商品编码正序排列

---

#### 八、批量操作规则
商品列表支持勾选后批量操作。

支持的批量操作包括：

- 批量删除
- 批量修改品类
- 批量修改品牌
- 批量修改自定义属性1
- 批量修改自定义属性2
- 批量修改自定义属性3
- 批量启用
- 批量停用

#### 批量操作方式
建议流程：

1. 勾选多条商品
2. 点击批量操作按钮
3. 选择具体操作类型
4. 弹窗确认
5. 执行并提示结果

#### 批量删除规则
- 删除前需二次确认
- 已被业务单据引用的商品不允许删除
- 删除失败时需提示原因

#### 批量修改规则
- 一次只允许修改一个字段
- 修改后提示成功数量和失败数量

---

#### 九、操作风格要求
商品管理模块整体采用表格型后台操作方式，要求：

- 以列表页操作为主
- 支持快速筛选
- 支持快速排序
- 支持多选和批量处理
- 尽量减少跳转详情页
- 更适合大量商品的日常维护和处理


# 第2章 基础资料配置模块

#### 基础资料配置模块定稿版

#### 一、模块定位
基础资料配置模块用于统一管理系统中反复使用的基础配置项。  
它不是业务单据模块，而是：

**为商品、采购、出库、上报、查询等模块提供统一基础数据来源的配置模块。**

该模块的目标是把那些经常被引用、反复被选择、需要统一口径的数据集中维护。

---

#### 二、模块作用
本模块至少解决以下问题：

- 品类从哪里来
- 品牌从哪里来
- 商品自定义属性的可选值从哪里来
- 采购自定义字段的可选值从哪里来
- 经销商固定信息从哪里来
- 哪些配置项启用
- 哪些配置项停用
- 哪些配置项还能继续被业务模块引用

---

#### 三、当前建议纳入的配置对象
当前建议先纳入以下配置对象：

#### 1. 品类配置
给商品模块使用。

#### 2. 品牌配置
给商品模块使用。

#### 3. 商品自定义属性配置
对应商品管理中的：

- 自定义属性1
- 自定义属性2
- 自定义属性3

#### 4. 采购自定义字段配置
对应采购入库中的：

- 采购自定义列1
- 采购自定义列2
- 采购自定义列3

#### 5. 经销商固定信息配置
对应上报模块中的：

- 经销商名称
- 经销商代码

该配置属于高优先级配置项，因为上报模块默认依赖它作为固定值来源。

---

#### 四、页面结构建议
基础资料配置模块建议采用：

- 左侧配置分类
- 右侧表格列表

#### 左侧配置分类建议包括：
- 品类配置
- 品牌配置
- 商品属性配置
- 采购字段配置
- 经销商配置

#### 右侧表格列表作用：
根据当前选中的配置分类，展示对应配置项列表。

---

#### 五、通用字段建议
大部分配置对象建议统一以下字段：

- 配置名称
- 配置编码
- 配置类型
- 状态
- 排序值
- 备注

---

#### 六、字段说明

#### 1. 配置名称
用于页面展示和用户识别。

#### 2. 配置编码
用于系统内部识别和检索。  
建议：

- 可手工填写
- 也可按规则自动生成
- 必须唯一

#### 3. 配置类型
用于区分当前配置属于哪一类，例如：

- 品类
- 品牌
- 商品属性
- 采购字段
- 经销商信息

#### 4. 状态
统一建议为：

- 启用
- 停用

#### 5. 排序值
用于控制页面显示顺序。

#### 6. 备注
用于补充说明。

---

#### 七、不同配置对象的专属字段建议

#### 1. 品类配置
建议字段：

- 品类名称
- 品类编码
- 状态
- 排序值
- 备注

#### 2. 品牌配置
建议字段：

- 品牌名称
- 品牌编码
- 状态
- 排序值
- 备注

#### 3. 商品自定义属性配置
建议拆成两层：

#### A. 属性名称配置
例如：
- 自定义属性1 现在叫“系列”
- 自定义属性2 现在叫“版本”
- 自定义属性3 现在叫“渠道”

#### B. 属性可选值配置
例如：
- “系列”下面有哪些可选项

建议字段包括：

- 属性名称
- 属性编码
- 属性值
- 属性值编码
- 状态
- 排序值
- 备注

#### 4. 采购自定义字段配置
同样建议拆成两层：

#### A. 字段名称配置
例如：
- 采购自定义列1
- 采购自定义列2
- 采购自定义列3

#### B. 字段可选值配置
例如：
- 每个字段下有哪些允许值

建议字段包括：

- 字段名称
- 字段编码
- 字段值
- 字段值编码
- 状态
- 排序值
- 备注

#### 5. 经销商固定信息配置
建议字段包括：

- 经销商名称
- 经销商代码
- 是否默认值
- 状态
- 备注

说明：
- 经销商名称和代码默认来自这里
- 可修改，但后续在业务页面中属于受控确认修改字段
- **是否默认值规则**：
  - 只能有一条记录设置为"是默认值"
  - 设置新默认值时，系统自动将原默认值改为非默认
  - 上报模块默认带入"是默认值"的经销商信息
  - 如需使用其他经销商信息，需在上报页面手动修改并确认

---

#### 八、状态规则
建议所有配置项统一使用两种状态：

- 启用
- 停用

#### 状态说明
- 启用：可在业务模块中继续被选择
- 停用：新业务不能继续选择，但历史数据继续保留显示

---

#### 九、操作规则
建议支持以下操作：

- 新增
- 编辑
- 删除
- 启用
- 停用
- 批量启用
- 批量停用

---

#### 十、删除规则
删除规则建议严格控制。

#### 1. 未被引用的配置项
可以删除。

#### 2. 已被业务引用的配置项
不允许删除，只允许：

- 编辑
- 停用

这样可以保证历史数据不乱。

---

#### 十一、编辑规则
建议如下：

- 名称可以修改
- 排序值可以修改
- 备注可以修改
- 状态可以修改
- 编码建议限制修改

如果已经被业务引用，建议进一步限制：

- 编码不允许修改
- 只允许修改名称、状态、备注、排序值

---

#### 十二、筛选规则
列表页建议支持以下筛选条件：

- 配置名称
- 配置编码
- 配置类型
- 状态

---

#### 十三、排序规则
建议支持按以下字段排序：

- 配置名称
- 配置编码
- 状态
- 排序值

默认排序建议：

- 先按排序值
- 再按名称正序

---

#### 十四、补充建议

#### 1. 是否已被引用
列表页建议增加字段：

- 是否已被业务引用

这样用户可以直接知道为什么不能删除。

#### 2. 引用数量
建议增加字段：

- 引用数量

例如：
- 某品牌被多少商品使用
- 某字段值被多少采购单使用

#### 3. 经销商固定信息单独高亮
由于经销商固定信息与上报模块直接相关，建议在配置分类中单独高亮展示。

---

#### 十五、模块总结
基础资料配置模块用于统一配置系统中的基础资料项，并作为商品、采购、出库、上报、查询等模块的标准数据来源。  
模块统一管理配置项的启用、停用、编辑和删除规则，确保业务模块引用口径一致，同时保证历史引用关系不丢失。


# 第3章 供应商管理模块

#### 供应商管理模块定稿版

#### 一、模块定位
供应商管理模块属于系统的基础资料模块，整体采用**表格型操作**方式。  
用于维护供应商基础信息，并控制该供应商名下的 SN 是否允许被提取。

---

#### 二、供应商字段
供应商管理当前字段包括：

- 供应商名称
- 供应商编码
- 是否允许提取 SN
- 状态

---

#### 三、字段规则

#### 1. 供应商名称
- 必填
- 用于识别供应商

#### 2. 供应商编码
- 由供应商中文名称的**拼音首字母自动生成**
- 用户填写供应商名称后，系统自动带出供应商编码
- 用于检索和识别

#### 3. 是否允许提取 SN
- 开关字段
- 开：该供应商名下的 SN 允许被提取
- 关：该供应商名下的 SN 不允许被提取

#### 4. 状态
- 启用
- 停用

用于控制该供应商资料是否继续可用。

---

#### 四、核心业务规则

#### 1. SN 提取开关是供应商维度
“是否允许提取 SN” 这个开关是**供应商维度**的控制规则，不是商品维度，也不是单个 SN 维度。

也就是说：

- 只要某供应商的提取开关关闭，则该供应商名下所有 SN 在任何提取场景下都不能被提取
- 提取时系统需自动跳过该供应商名下 SN
- 只有提取开关开启的供应商，其名下 SN 才能参与提取

#### 2. 供应商与采购单的关系
- 一张采购单对应一个供应商
- 采购单下所有商品明细默认属于该供应商
- 采购单下所有 SN 也都归属于该供应商链路
- 后续 SN 是否允许提取，要受该供应商提取开关控制

#### 3. 状态与提取开关是两个独立概念
- 状态：控制该供应商资料是否可继续使用
- 是否允许提取 SN：控制该供应商名下 SN 是否允许参与提取

两者不能混用。

---

#### 五、列表页展示
供应商管理模块采用表格列表展示。

建议列表字段包括：

- 多选框
- 供应商名称
- 供应商编码
- 是否允许提取 SN
- 状态
- 操作

操作包括：

- 编辑
- 删除
- 开启提取
- 关闭提取
- 启用
- 停用

---

#### 六、筛选规则
列表页顶部支持筛选。

支持筛选字段包括：

- 供应商名称
- 供应商编码
- 是否允许提取 SN
- 状态

筛选方式建议：

- 供应商名称：输入搜索
- 供应商编码：输入搜索
- 是否允许提取 SN：下拉选择
- 状态：下拉选择

---

#### 七、排序规则
列表页支持按以下字段排序：

- 供应商名称
- 供应商编码
- 是否允许提取 SN
- 状态

默认排序建议：

- 按供应商名称正序

---

#### 八、批量操作规则
供应商列表支持勾选后批量操作。

支持的批量操作包括：

- 批量删除
- 批量开启提取
- 批量关闭提取
- 批量启用
- 批量停用

---

#### 九、删除规则
建议规则如下：

- 若供应商已被采购单引用，则不允许直接删除
- 已被业务引用的供应商只能编辑或停用，不能删除

---

#### 十、开关操作规则
“是否允许提取 SN”支持：

- 单个操作
- 批量操作

并且操作后立即生效。

规则如下：

- 关闭后，该供应商名下所有 SN 后续都不能被提取
- 开启后，该供应商名下符合条件的 SN 才可以参与提取

---

#### 十一、操作风格要求
供应商管理模块整体采用表格型后台操作方式，要求：

- 以列表页操作为主
- 支持快速筛选
- 支持快速排序
- 支持多选和批量处理
- 尽量减少跳转详情页


# 第4章 采购入库模块

#### 采购入库模块定稿版

#### 一、模块定位
采购入库模块用于创建和管理采购入库单，整体采用**表格型操作**方式。  
支持在一张采购单中录入多个商品，并支持每个商品对应的 SN（序列号）上传、补录和追溯。

---

#### 二、采购入库单主表字段
采购入库单主表当前字段包括：

- 采购单号
- 入库时间
- 供应商
- 采购单状态（草稿 / 已入库 / 已红冲）
- 备注

#### 采购单状态说明
- 草稿：采购单仍在编辑中，未正式入库
- 已入库：采购单已确认入库，商品已进入库存
- 已红冲：采购单已被红冲撤销，保留记录但不再生效

---

#### 三、采购商品明细字段
每一行采购商品明细包括：

- 商品编码
- 商品名称
- 颜色
- 品类
- 品牌
- 采购数量
- 采购价格
- 价格类型
- 采购自定义列1
- 采购自定义列2
- 采购自定义列3
- 已录入 SN 数量
- 待补录数量
- SN 补录状态
- SN 上传入口

---

#### 四、商品选择规则
在新建采购单时，商品支持以下两种选择方式：

- 按商品名称选择
- 按商品编码选择

任意一种方式选中商品后，系统自动带出该商品信息。

---

#### 五、自动带出规则
选中商品后，系统自动带出以下内容：

#### 1. 商品基础信息
- 商品编码
- 商品名称
- 颜色
- 品类
- 品牌

#### 2. 最近一次采购信息
根据该商品最近一次采购记录，自动带出：

- 最近一次采购价格
- 最近一次价格类型
- 最近一次采购自定义列1
- 最近一次采购自定义列2
- 最近一次采购自定义列3

用户可在当前采购单中继续修改这些值。

---

#### 六、采购价格规则
每个商品在采购时都需要填写采购价格。

规则如下：

- 采购价格为手工填写
- 同商品再次采购时，默认带出上一次采购价格
- 用户可以修改本次采购价格

---

#### 七、价格类型与采购自定义列规则

#### 1. 价格类型
采购明细中有 1 个固定字段：

- 价格类型

价格类型可选值固定为：

- 原价
- 特价
- 一口价

规则如下：

- 默认带出该商品最近一次采购时的价格类型
- 用户可以修改
- 该字段属于采购明细字段，不属于商品主数据字段
- **说明**：采购入库价格类型不包含"不限"，因为采购时必须明确价格类型，不允许模糊处理。出库和上报时可选择"不限"，表示不区分价格类型进行提取或筛选

#### 2. 采购自定义列
采购明细中另外有 3 个采购维度的自定义列：

- 采购自定义列1
- 采购自定义列2
- 采购自定义列3

规则如下：

- 默认带出该商品最近一次采购时对应的值
- 可以为空
- 可以不选
- 用户可以修改
- 该 3 列属于采购明细字段，不属于商品主数据字段

---

#### 八、供应商规则
- 每张采购入库单必须选择一个供应商
- 一张采购单对应一个供应商
- 采购单下所有商品明细默认属于该供应商
- 采购单下所有 SN 也都归属到该供应商链路下
- 后续 SN 是否允许提取，需要受该供应商的提取开关控制

---

#### 九、SN 管理规则

#### 1. SN 定义
SN 即序列号。

#### 2. SN 唯一性
SN 必须全局唯一，不允许重复。

#### 3. SN 归属关系
SN 必须挂在具体的采购商品明细下。  
也就是说，SN 不只是对应商品，还必须对应到某一次具体采购。

#### 4. SN 溯源要求
每一个 SN 都必须可以追溯到：

- 所属商品
- 所属供应商
- 所属采购单
- 所属采购明细
- 当次采购价格
- 当次价格类型
- 当次采购自定义列信息

后续系统还需要支持通过 SN 查询完整日志和历史流转记录。

---

#### 十、SN 上传规则
每个采购商品明细行都支持上传对应的 SN。

支持的上传方式包括：

- Excel
- TXT
- CSV
- 图片

同时支持：

- 首次上传
- 分批上传
- 后续补录

---

#### 十一、建单方式
采购入库模块支持两种建单方式：

#### 1. 页面手工建单
通过页面新增采购单，并在表格中逐行选择商品、填写数量、价格和采购属性。

#### 2. 表格导入建单
通过导入采购信息表格，自动生成采购入库单及商品明细。  
如导入内容中包含 SN，也可同步导入对应 SN。

---

#### 十二、采购数量与 SN 数量关系
采购商品的数量与 SN 数量原则上应一一对应。

例如：

- A 商品采购数量 100，则理论上应有 100 个 SN
- B 商品采购数量 50，则理论上应有 50 个 SN

---

#### 十三、SN 未补全时的处理规则
若采购商品当前未补齐全部 SN，仍允许先完成采购入库单。

例如：

- 采购数量 100
- 当前仅录入 80 个 SN

则该商品仍可入库，但系统需记录：

- 已录入 SN 数量：80
- 待补录数量：20
- SN 补录状态：部分录入

---

#### 十四、SN 补录状态
建议采购商品明细增加 SN 补录状态字段，状态包括：

- 未录入
- 部分录入
- 已补齐

用于清晰展示每一行商品的 SN 完整情况。

---

#### 十五、操作规则

#### 1. 手工建单流程
1. 新建采购入库单
2. 填写入库时间、选择供应商、填写备注
3. 在明细表中选择商品
4. 可通过商品名称或商品编码选中商品
5. 系统自动带出商品基础信息
6. 系统自动带出最近一次采购价格、价格类型和采购自定义列
7. 用户填写或修改本次采购数量、价格、价格类型和自定义列
8. 按商品行上传 SN，或后续再补录
9. 保存采购入库单

#### 2. 导入建单流程
1. 导入采购信息表格
2. 系统自动生成采购入库单
3. 自动生成采购商品明细
4. 如包含 SN，则同步导入 SN 数据

---

#### 十六、校验规则

#### 1. 供应商校验
- 采购入库单必须选择供应商
- 未选择供应商时，不允许保存

#### 2. 商品选择校验
- 商品名称或商品编码选中后，必须唯一定位到商品
- 商品不存在时，不允许保存

#### 3. 采购数量校验
- 采购数量必须填写
- 采购数量必须大于 0

#### 4. 采购价格校验
- 采购价格必须填写
- 采购价格必须大于等于 0

#### 5. 价格类型校验
- 价格类型可选值仅限：原价、特价、一口价

#### 6. SN 唯一性校验
- SN 全局唯一
- 不允许重复录入
- 不允许与系统历史 SN 重复

#### 7. SN 数量校验
- 单个商品的 SN 总录入数量不能超过采购数量
- 可以少于采购数量
- 少于采购数量时，允许先保存和完成采购入库单
- 系统必须记录待补录数量

#### 8. 补录校验
- 后续补录时，累计补录数量不能超过采购数量
- 补录完成后，待补录数量更新为 0
- 补录完成后，SN 补录状态变为“已补齐”

---

#### 十七、列表与显示要求
采购入库模块应采用表格型操作风格，支持：

- 一张单中录入多个商品
- 商品按表格形式展示
- 每行显示 SN 进度
- 每行提供 SN 上传入口
- SN 未补全的商品高亮提示

---

#### 十八、与后续库存模块的衔接
采购入库完成后，后续库存模块中需能体现：

- 总采购数量
- 已录入 SN 数量
- 待补录数量

例如：

- 总数 100
- 已录入 80
- 待补录 20

其中待补录商品需做高亮提示。


# 第5章 库存查询模块

#### 库存查询模块定稿版

#### 一、模块定位
库存查询模块用于查看**当前库存状态**，整体采用**表格型操作**方式。  
模块以商品维度展示当前库存情况，并提供筛选、排序、多选、导出、补录 SN 等能力。

---

#### 二、统计口径

#### 1. 库存数量
- 按**商品维度**统计
- 不受 SN 是否补全影响

#### 2. 库存金额
- 按**商品维度**统计
- 不按 SN 维度计算
- 不受 SN 是否补全影响

#### 3. 待补录 SN
- 单独展示待补录数量
- 只作为附加提示信息
- 不影响库存数量和库存金额

---

#### 三、列表基础能力
库存查询模块支持：

- 多选
- 批量筛选
- 排序
- 导出

---

#### 四、列表展示字段
库存查询列表建议展示以下字段：

- 多选框
- 商品编码
- 商品名称
- 颜色
- 品类
- 品牌
- 当前库存数量
- 当前库存金额
- 已录入 SN 数量
- 待补录 SN 数量
- 库存状态
- 最近入库时间
- 最近一次采购价格
- 操作

操作包括：

- 补录 SN
- 查看明细
- 查看相关日志

---

#### 五、筛选规则
库存查询页顶部支持筛选。

#### 1. 默认筛选字段
建议支持以下筛选条件：

- 商品编码
- 商品名称
- 颜色
- 品类
- 品牌
- 供应商
- 库存状态
- 是否只看待补录
- 最近入库时间

#### 2. 可选筛选字段
还需支持以下字段参与筛选：

- 价格类型
- 自定义字段

规则如下：

- 价格类型默认不在列表展示
- 自定义字段默认不在列表展示
- 需要时可手动选择用于筛选或显示

---

#### 六、排序规则
库存查询页支持按以下字段排序：

- 商品编码
- 商品名称
- 当前库存数量
- 当前库存金额
- 待补录 SN 数量
- 库存状态
- 最近入库时间
- 最近一次采购价格

默认排序建议：

- 按商品编码正序

---

#### 七、库存状态规则
库存查询模块中增加库存状态字段，用于快速识别和筛选商品。

建议状态包括：

- 有库存
- 零库存
- 待补录
- 异常

说明：

- “待补录”表示该商品仍有未补录完成的 SN
- “异常”用于识别特殊库存问题，判定条件包括：
  - SN数量与库存数量不一致（已录入SN数量 > 库存数量）
  - 存在重复SN记录
  - SN归属链路断裂（无法追溯到采购单）
  - 库存数量为负数

---

#### 八、待补录 SN 规则
库存查询页中，待补录 SN 不能只做高亮提示，还必须明确显示数量。

例如：

- 当前库存数量：100
- 已录入 SN 数量：80
- 待补录 SN 数量：20

规则如下：

- 只要待补录 SN 数量大于 0，该商品行需高亮提示
- 库存查询页支持直接补录 SN

---

#### 九、供应商筛选规则
库存查询页支持按供应商筛选。

说明：

- 库存商品来自采购入库记录
- 每张采购单对应一个供应商
- 因此库存查询应支持按供应商查看库存情况

---

#### 十、最近入库时间规则
列表中增加：

- 最近入库时间

用于查看该商品最近一次入库的时间，方便判断库存的新旧情况。

---

#### 十一、最近一次采购价格规则
列表中增加：

- 最近一次采购价格

用于辅助用户理解当前库存金额的来源。

---

#### 十二、只看待补录规则
库存查询页增加快捷筛选条件：

- 只看待补录商品

用于快速定位 SN 未补全的商品。

---

#### 十三、导出规则
库存查询页支持导出当前筛选结果。

可用于导出：

- 当前库存列表
- 待补录商品列表
- 某类筛选条件下的库存结果

---

#### 十四、查看明细规则
每一行商品支持：

- 查看明细

用于查看该商品相关的库存明细信息，例如：

- 采购记录
- SN 录入情况
- 待补录情况

---

#### 十五、查看相关日志规则
每一行商品支持：

- 查看相关日志

用于查看该商品相关的库存变化、SN 补录等操作记录。

---

#### 十六、操作风格要求
库存查询模块整体采用表格型后台操作方式，要求：

- 以列表页操作为主
- 支持快速筛选
- 支持快速排序
- 支持多选和批量处理
- 支持直接补录 SN
- 尽量减少跳转详情页

---

#### 十七、模块总结
库存查询模块用于查看当前商品库存情况，按商品维度统计库存数量和库存金额。  
SN 补录情况作为附加管理信息展示，包括已录入数量、待补录数量及高亮提示。  
模块同时支持供应商筛选、库存状态筛选、价格类型和自定义字段筛选，并支持导出、查看明细和查看相关日志。


# 第6章 商品出库模块

#### 商品出库模块定稿版

#### 一、模块定位
商品出库模块是当前版本最核心的模块。  
模块用于选择商品进行出库，并在出库过程中处理对应数量的 SN（序列号）。

模块整体延续前面统一的操作方式：

- 表格型操作
- 支持筛选
- 支持排序
- 支持批量处理
- 尽量少跳转详情页

页面主动作统一为：

- 确认出库

其中内部 SN 处理逻辑统一表述为：

- 提取 SN

---

#### 二、出库对象
一次出库可以选择多个商品。

例如：

- A 商品
- B 商品
- C 商品

每个商品都有自己的出库数量，并需要处理对应数量的 SN。

---

#### 三、出库页面字段
出库页面中，每个商品建议至少包括以下字段：

- 商品编码
- 商品名称
- 颜色
- 品类
- 品牌
- 出库数量
- 非促销价
- 促销价
- SN 提取类型
- 已提取 SN 数量
- SN 提取结果
- 操作

---

#### 四、价格规则
每个商品在出库页面中需要填写两列价格：

- 非促销价
- 促销价

规则如下：

- 默认值都为 0
- 支持手工填写
- 下一次同商品出库时，自动带出上一次填写的值
- 价格不得小于 0

---

#### 五、SN 提取逻辑

#### 1. 默认提取顺序
出库时，SN 默认按：

- 从最早到最新

即按最早的优先进行提取。

#### 2. 按价格类型筛选提取
出库时支持按价格类型筛选 SN，价格类型包括：

- 原价
- 特价
- 一口价
- 不限

规则如下：

- 若选择“原价 / 特价 / 一口价”，则只在该类型范围内按最早到最新提取
- 若不选择，则按“不限”处理
- “不限”表示不区分价格类型，直接按顺序提取

#### 3. 筛选方式
SN 提取类型支持两种设置方式：

#### 单个商品设置
每个商品行后面可单独设置提取条件。

#### 整单批量设置
可对整张出库单中的所有商品统一设置同一个提取条件。

---

#### 六、汇总规则
出库页面中应实时汇总并显示：

- 总商品数量
- 总金额

总金额根据页面中填写的价格和数量自动计算。

---

#### 七、确认前预览建议
确认出库前，建议系统增加 SN 提取结果预览，至少显示：

- 本次将提取多少个 SN
- 每个商品是否满足出库数量
- 是否存在指定类型 SN 不足
- 是否存在待补录 SN
- 是否存在不可提取情况

---

#### 八、确认出库后的分流
点击“确认出库”后，系统弹出提示：

- 是否转入 SN 上报表

#### 1. 选择“否”
若用户选择不转入 SN 上报表，则：

- 系统直接完成出库
- 自动生成 Excel 文件
- 输出内容包括：
  - 商品明细
  - 序列号明细
  - 价格明细

流程到此结束。

#### 2. 选择"是"（以rules/001-04为唯一真源）
若用户选择转入 SN 上报表，则：

- 出库状态变为"待上报确认"
- 创建上报单草稿，带入商品信息、数量、价格、提取类型等条件
- 不正式提取SN
- 不完成出库
- 不生成任何文件
- 进入序列号上报页面后可二次修改
- 上报页面最终确认时才正式提取SN + 完成出库

> 注意：本节以 rules/001 中 001-04 和"主流程裁决说明"为最高优先级。
> 不再使用"已出库"、"已转上报"等旧版状态语义。

---

#### 九、出库单主表字段
出库单主表建议包括：

- 出库单号
- 出库时间
- 备注
- 是否转入上报表
- 出库总商品数量
- 出库总金额
- 出库状态

---

#### 十、出库商品明细字段
每一行商品明细建议包括：

- 商品编码
- 商品名称
- 颜色
- 品类
- 品牌
- 出库数量
- 非促销价
- 促销价
- SN 提取类型
- 已提取 SN 数量
- SN 提取结果
- 操作

#### SN 提取类型
可选值包括：

- 原价
- 特价
- 一口价
- 不限

#### SN 提取结果
建议状态包括：

- 提取成功
- 数量不足
- 部分提取
- 待处理

---

#### 十一、出库单状态

> 以 rules/001 "状态命名约束" 为唯一真源，以下为统一后的状态。

出库单状态包括：

- 草稿
- 待确认
- 待上报确认
- 已完成
- 已红冲

#### 状态说明
- 草稿：单据仍在编辑中
- 待确认：内容已填好，等待最终确认
- 待上报确认：用户选择转入上报，出库尚未完成，等待上报页面最终确认
- 已完成：出库已完成（无论是否经过上报流程）
- 已红冲：该出库单已被红冲撤销，保留记录但不再生效

注意：不再使用"已出库"、"已转上报"等旧状态名。

---

#### 十二、商品行 SN 提取状态建议
每个商品行建议增加 SN 提取状态：

- 未提取
- 提取中
- 已提取
- 部分提取
- 提取失败

---

#### 十三、基础校验规则

#### 1. 商品校验
- 出库单里必须有商品明细
- 商品必须存在
- 商品必须能唯一定位

#### 2. 出库数量校验
- 出库数量必须填写
- 出库数量必须大于 0
- 出库数量不能超过当前可出库数量

#### 3. 价格校验
- 非促销价必须填写
- 促销价必须填写
- 两个价格默认可为 0
- 价格不得小于 0

#### 4. SN 提取类型校验
SN 提取类型可选值仅限：

- 原价
- 特价
- 一口价
- 不限

---

#### 十四、SN 提取校验规则

#### 1. 默认提取规则
- 未指定提取类型时，按“不限”处理
- 系统按最早到最新顺序提取

#### 2. 指定类型提取规则
- 若指定原价 / 特价 / 一口价，则只能从该类型中提取
- 提取顺序仍为最早到最新

#### 3. 数量校验
- 成功提取的 SN 数量必须与出库数量一致，才允许完整出库
- 若不足，则进入异常提示或部分提取逻辑

---

#### 十五、确认出库前校验
点击“确认出库”前，系统至少应检查：

- 每个商品是否有足够数量可出库
- 每个商品在当前提取类型下是否有足够 SN
- 是否存在待补录 SN 影响本次提取
- 是否存在供应商禁止提取的情况
- 是否存在商品已无可用 SN 的情况

---

#### 十六、异常场景

#### 1. 商品库存不足
例如：
- 要出库 100
- 当前可出库只有 80

处理建议：
- 不允许确认出库
- 明确提示库存不足

#### 2. 总库存够，但指定类型 SN 不足
例如：
- 商品总可出库 100
- 但“特价”类型只有 30
- 用户要求按特价提取 50

处理建议：
- 不允许按当前条件直接完成
- 提示“当前价格类型下 SN 数量不足”

#### 3. 商品存在待补录 SN
例如：
- 商品库存数量有 100
- 但其中有 20 个待补录

处理建议：
- 页面提示该商品存在待补录 SN
- 后续是否允许继续出库，需根据最终业务规则进一步明确

#### 4. 供应商不允许提取
若该商品对应供应商禁止提取，则：

- 不允许提取相关 SN
- 明确提示原因

#### 5. 重复出库风险
若同一批 SN 已被处理过，则不能再次提取。

处理建议：
- 系统校验 SN 当前状态
- 已出库 / 已占用 / 已上报的 SN 不能再次参与当前出库

---

#### 十七、确认出库后的校验分支

#### 1. 选择"不转入上报表"
需要校验：

- 是否已成功提取足够SN
- 是否可正常生成 Excel 明细文件

#### 2. 选择"转入上报表"
需要校验：

- 当前出库数据是否完整可流转到上报页面
- 注意：此时不校验SN提取结果，因为SN尚未提取
- SN提取将在上报页面最终确认时进行

> 以 rules/001 "主流程裁决说明" 为准。

---

#### 十八、出库单红冲规则

#### 1. 支持红冲的场景
以下场景下，出库单支持红冲：

- 已确认出库
- 已生成导出表格
- 但后续确认该次出库无效
- 或该次出库需要整体撤销

#### 2. 出库单红冲后的处理
当出库单执行红冲后，系统应执行：

- 出库单状态改为已红冲
- 原出库数量对应的商品库存全部回退
- 原出库提取的 SN 全部回退到库存可用状态
- SN 恢复到红冲前对应的原始链路关系
- 原导出的 Excel 文件保留，但标记为已作废
- 系统记录红冲时间、红冲人、红冲原因

#### 3. 出库单红冲限制
建议规则：

- 不允许直接删除已确认出库单
- 已确认出库单如需撤销，只能走红冲
- 红冲后，该单不可再次编辑，只允许查看
- 如存在后续关联单据，需按联动规则一并处理

#### 4. 出库单红冲的联动规则
如果该出库单已经转入上报页面，甚至已经生成上报单，则：

- 不允许只单独删除出库动作
- 应按照联动撤销规则处理
- 即优先撤销上报链路，再整体回退出库链路

---

#### 十九、统一日志要求
出库单红冲必须记录：

- 单据编号
- 红冲时间
- 红冲操作人
- 红冲原因
- 回退的商品数量
- 回退的 SN 数量
- 关联的原始单据编号
- 是否联动撤销其他单据

---

#### 二十、补充建议
建议后续补充明确以下规则：

- 当指定类型 SN 不足时，是否允许用户一键改成“不限”重新提取
- 自动生成的 Excel 文件命名规则
- 是否一张出库单生成一个文件还是多个文件
- 转入上报页面时，哪些数据需原样带入


# 第7章 序列号上报模块

#### 序列号上报模块定稿版

#### 一、模块定位
序列号上报模块用于承接商品出库模块中“转入 SN 上报表”的业务分支。  
该模块不是重新录入数据，而是在继承出库页面信息的基础上，进行二次筛选、二次确认、日期分配，并最终按固定模板生成上报表。

模块整体延续前面统一的操作方式：

- 表格型操作
- 支持筛选
- 支持排序
- 支持批量处理
- 以商品维度作为进入页面的起始展示方式

---

#### 二、页面字段与导出字段的区分

#### 1. 页面字段
页面字段用于：

- 操作
- 筛选
- 确认
- 分配
- 修改

因此页面字段可以多于最终导出字段。

#### 2. 导出字段
最终导出字段严格按固定模板输出，不修改模板表头、列顺序和格式。  
系统只负责将最终确认后的数据填入模板。

固定导出字段包括：

- 经销商名称
- 经销商代码
- BOSE 产品编号
- 发货日期
- 销量
- 非促销价格
- 促销价格
- SN 编码

其中：

- BOSE 产品编号 = 商品编码

---

#### 三、与出库模块的关系

#### 1. 默认继承出库数据（以rules/001为唯一真源）
转入上报页面后，系统默认继承出库页面已确认的信息，包括：

- 商品信息
- 出库数量
- 非促销价
- 促销价
- 价格类型

> 注意：不继承已提取的SN，因为转上报时SN尚未提取。继承的是选择条件，不是提取结果。

#### 2. 可做二次修改
虽然默认继承，但在上报页面中仍允许做二次修改和确认。  
修改后，需要再次确认才生效。

#### 3. 重新继承建议
建议支持“恢复出库页默认值”或“重新继承出库数据”的操作，便于用户纠错。

---

#### 四、经销商字段规则

#### 1. 经销商名称
- 默认固定值
- 默认锁定
- 点击修改并确认后才允许修改

#### 2. 经销商代码
- 默认固定值
- 默认锁定
- 点击修改并确认后才允许修改

#### 3. 经销商字段说明
经销商名称和经销商代码属于：

- 默认只读字段
- 受控修改字段

不是普通自由编辑字段。

---

#### 五、发货日期规则

#### 1. 发货日期定义
发货日期是上报表中的发货日期。  
它与采购日期、入库日期无直接关系。

#### 2. 整单日期范围
进入上报页面后，先为整单统一选择一个发货日期范围。

规则如下：

- 默认可给一个周期
- 也支持用户自定义日期范围
- 日期范围不能为空

#### 3. 商品级日期集合
每个商品默认继承整单日期范围。  
同时，每个商品也可以单独指定自己的可用上报日期集合。

规则如下：

- 商品单独选择的日期必须落在整单日期范围内
- 不允许超出整单范围

---

#### 六、SN 日期分配规则

#### 1. 分配原则
最终上报时：

- 1 个 SN = 1 行
- 1 行对应 1 个发货日期

#### 2. 分配方式
SN 在允许的日期范围或日期集合中进行：

- 50% 平均分配
- 50% 随机分配

也就是说：

- 不是完全随机
- 也不是绝对平均
- 而是在整体均衡基础上保留一定随机性

#### 3. 分配结果要求
- 每个 SN 必须被分配到一个发货日期
- 不允许一个 SN 没有日期
- 不允许一个 SN 对应多个日期

---

#### 七、价格规则

#### 1. 非促销价 / 促销价
- 默认继承出库页面
- 可以二次修改
- 修改后需确认
- 允许为 0
- 不允许小于 0

#### 2. 价格类型
价格类型包括：

- 原价
- 特价
- 一口价
- 不限

规则如下：

- 默认继承出库页面转入前的选择
- 可在上报页面二次修改
- 修改后需确认

#### 3. 修改后的影响
修改价格类型后：

- 旧的预览结果立即失效
- 需要重新预览才能看到新的候选结果
- 只有最终确认（confirm）才会正式落库

> 以 rules/001 "上报确认裁决" 为准。

---

#### 八、上报单主表字段
建议先有这些字段：

- 上报单号
- 关联出库单号
- 经销商名称
- 经销商代码
- 整单发货日期范围
- 是否已修改经销商信息
- 上报总商品数量
- 上报总 SN 数量
- 上报状态
- 备注

#### 上报单号生成规则
上报单号定义为：**该次上报发货日期范围中的最后一天**

例如：
- 某次上报发货日期范围最后一天为 `2026-04-06`
- 则该次上报单号默认为 `2026-04-06`

若同一天存在多次上报，则在该日期后追加序号区分：
- 2026-04-06-1
- 2026-04-06-2
- 2026-04-06-3

以此类推。

---

#### 九、上报商品明细字段
每一行商品建议有：

- 商品编码
- 商品名称
- 颜色
- 品类
- 品牌
- 上报数量
- 非促销价
- 促销价
- 价格类型
- 整单默认日期范围
- 商品可用上报日期
- 已分配 SN 数量
- 分配状态
- 操作

---

#### 十、上报页面状态建议

#### 1. 上报单状态（以rules/001为唯一真源）

| 状态 | 含义 |
|------|------|
| 草稿 | 刚从出库页转入，仍在编辑中 |
| 待确认 | 条件已填好，等待用户确认 |
| 待生成 | 预览已确认，等待生成文件 |
| 已生成 | 文件已生成 |
| 已完成 | 整个上报流程完成（同时出库状态变为已完成） |
| 已红冲 | 已被红冲撤销 |

注意：上报最终确认（confirm）是唯一正式落库的入口，同时完成SN提取+日期分配+生成表格+出库完成。

#### 3. 商品行分配状态
建议每个商品行增加状态：

- 未分配
- 分配中
- 已分配
- 部分分配
- 分配失败

#### 4. 经销商信息状态
建议单独增加轻状态：

- 默认值
- 已修改待确认
- 已确认修改

---

#### 十一、上报页面校验规则

#### 1. 主表校验
- 上报单必须来源于一张已确认转入的出库单
- 没有关联出库单，不允许生成上报表
- 经销商名称默认固定值必须存在
- 经销商代码默认固定值必须存在
- 若用户触发修改，则必须确认后才能生效
- 整单发货日期范围必须选择

#### 2. 商品明细校验
- 商品编码必须存在
- 商品编码直接映射 BOSE 产品编号
- 上报数量必须大于 0
- 上报数量不能超过已提取 SN 数量
- 非促销价和促销价必须有值
- 价格类型仅允许为原价 / 特价 / 一口价 / 不限

#### 3. 日期分配校验
- 每个商品所选日期必须在整单范围内
- 每个商品被分配日期的 SN 数量必须等于该商品上报数量
- 每个 SN 必须分配到一个日期
- 不允许一个 SN 对应多个日期

#### 4. 导出前校验
最终生成模板表格前，系统至少要校验：

- 经销商名称和代码是否已确认
- 商品编码是否完整
- 每个商品的上报数量是否满足
- 每个 SN 是否已分配发货日期
- 最终导出字段是否完整
- 是否满足固定模板要求

---

#### 十二、异常场景

#### 1. 经销商信息被修改但未确认
处理建议：
- 不允许生成表格
- 提示“经销商信息修改未确认”

#### 2. 商品没有可用日期
处理建议：
- 不允许完成生成
- 提示“该商品未设置有效上报日期”

#### 3. 商品可用日期超出整单范围
处理建议：
- 不允许保存当前设置
- 提示“商品日期必须在整单范围内”

#### 4. SN 数量不足
处理建议：
- 不允许最终生成表格
- 提示“SN 分配数量不足”

#### 5. 修改价格类型后引发 SN 不匹配
处理建议：
- 不允许直接生成
- 提示“修改后的价格类型下 SN 数量不足”

#### 6. 最终导出字段缺失
处理建议：
- 不允许生成模板表格
- 明确指出缺失字段

---

#### 十三、导出规则

#### 1. 固定模板导出
最终生成的上报表：

- 使用固定模板表头
- 不修改模板表头
- 不修改列顺序
- 不修改模板格式

#### 2. 系统生成内容
系统仅负责将最终确认后的数据填入模板中。  
页面字段不等于导出字段，最终导出只保留模板要求的固定列。

#### 3. 导出前建议预览
建议正式导出前增加“导出前预览表”，仅展示最终导出字段，便于用户最后确认。

---

#### 十四、上报单红冲规则

#### 1. 支持红冲的场景
以下场景下，上报单支持红冲：

- 已从出库单转入上报页面
- 已完成上报数据确认
- 已生成上报表
- 但后续确认本次不上报
- 或整次上报需要撤销

#### 2. 上报单红冲采用方案 B
上报单红冲时，不只是撤销上报，还要连带撤销对应出库。

也就是说：

- 上报作废
- 出库作废
- SN 回退
- 库存回退

全部一起完成。

#### 3. 上报单红冲后的处理
当上报单执行红冲后，系统应执行：

- 上报单状态改为已红冲
- 该上报单关联的出库链路一并撤销
- 所有关联 SN 按原始信息全部回退到库存
- 商品库存数量全部回退
- 原生成的上报表文件保留，但标记为已作废
- 记录红冲时间、红冲人、红冲原因

#### 4. 上报单红冲后的数据回退要求
回退时要恢复原链路信息，包括但不限于：

- 商品归属
- 供应商归属
- 原采购链路
- 原价格类型
- 原采购价格
- 原库存可用状态

也就是说：

- 回退不是简单加库存数量
- 而是按原始业务链路恢复

#### 5. 上报单红冲限制
建议规则：

- 不允许直接删除已生成上报单
- 已生成上报单如需取消，只能红冲
- 红冲后单据不可编辑，只允许查看
- 所有红冲动作必须留日志

---

#### 十五、统一日志要求
无论是上报单红冲还是因联动导致出库撤销，都必须记录：

- 单据编号
- 红冲时间
- 红冲操作人
- 红冲原因
- 回退的商品数量
- 回退的 SN 数量
- 关联的原始单据编号
- 是否联动撤销其他单据

---

#### 十六、补充建议
建议后续补充明确以下规则：

- 修改价格类型后，是否重新筛选 SN
- 导出文件命名规则
- 日期分配结果的预览方式
- 页面字段与导出字段的正式映射清单


# 第8章 SN全局查询模块

#### SN 全局查询模块定稿版

#### 一、模块定位
SN 全局查询模块用于对单个或多个 SN 进行精准查询和批量查询。  
模块目标是查看 SN 的**全生命周期追溯信息**，并且这些信息：

- 不允许删除
- 只能追加
- 必须可追溯

---

#### 二、查询方式
支持两种查询方式：

#### 1. 精准查询
- 输入单个 SN
- 返回该 SN 的完整追溯结果

#### 2. 批量查询
- 一次输入多个 SN
- 支持换行、逗号、粘贴列表
- 自动去重
- 返回多个 SN 的批量结果

---

#### 三、主信息字段
每个 SN 的查询结果建议展示：

- SN 编码
- 商品编码
- 商品名称
- 颜色
- 品类
- 品牌
- 当前状态
- 当前所在环节
- 当前是否可用

---

#### 四、采购链路字段
建议展示：

- 采购单号
- 入库时间
- 供应商名称
- 供应商编码
- 采购价格
- 价格类型
- 采购自定义列1
- 采购自定义列2
- 采购自定义列3

---

#### 五、出库链路字段
建议展示：

- 当前是否在库
- 当前库存状态
- 出库单号
- 出库时间
- 非促销价
- 促销价
- 出库时价格类型
- 当前是否已出库

---

#### 六、上报链路字段
建议展示：

- 是否已转入上报
- 上报单号
- 上报日期范围
- 实际发货日期
- 上报价格
- 是否已生成上报表

---

#### 七、红冲与回退字段
建议展示：

- 是否已红冲
- 红冲单据类型
- 红冲时间
- 红冲原因
- 是否已回退库存
- 回退后当前状态

---

#### 八、日志字段
建议单独展示日志区：

- 操作时间
- 操作类型
- 单据编号
- 所属模块
- 操作说明
- 操作人

并按时间顺序展示完整时间线。

---

#### 九、SN 状态建议
建议当前主状态限定为：

- 在库
- 已出库
- 已上报
- 已红冲
- 已回退
- 异常

规则：
- 同一时刻只能有一个当前主状态
- 历史状态通过日志追溯

#### 异常状态判定条件
SN状态为"异常"的判定条件包括：

- SN无法追溯到采购单（采购链路断裂）
- SN状态与实际业务链路冲突（如在库但实际已出库）
- SN归属商品与实际记录不一致
- SN存在重复记录
- SN红冲后未正确回退

---

#### 十、基础校验规则

#### 1. 精准查询校验
- 输入不能为空
- SN 格式必须合法
- 一次只查一个 SN

#### 2. 批量查询校验
- 批量输入不能为空
- 自动去重
- 超过上限时提示分批查询

#### 3. 存在性校验
- SN 必须存在
- 不存在的 SN 单独提示
- 批量查询时成功和失败分开标识

---

#### 十一、追溯校验规则
- 已采购的 SN 必须能看到采购链路
- 已出库的 SN 必须能看到出库链路
- 已上报的 SN 必须能看到上报链路
- 已红冲的 SN 必须能看到红冲链路
- 历史记录不允许删除
- 日志必须完整

---

#### 十二、异常场景
建议至少支持识别：

- SN 不存在
- 链路不完整
- 状态与链路冲突
- 批量查询部分失败
- SN 链路重复
- 日志缺失

---

#### 十三、补充建议
建议后续增加：

- 批量查询结果导出
- 只看异常 SN
- 查看完整时间线
- 成功 / 异常 / 未找到分组展示

---

#### 十四、模块总结
SN 全局查询模块是系统的全链路追溯入口，用于对 SN 做精准或批量查询，查看其采购、出库、上报、红冲、回退及完整日志信息，且历史记录不可删除。


# 第9章 全局进销查询模块

#### 全局进销查询模块定稿版

#### 一、模块定位
全局进销查询模块用于按不同业务时间维度，对系统中的商品进销数据进行统一查询。  
模块支持按不同时间口径查询，并根据当前查询维度展示对应的商品数量、金额及相关统计结果。

该模块不是单一库存页，也不是单一流水页，而是：

**面向全局业务数据的多维度汇总查询模块。**

---

#### 二、查询维度
当前已明确支持以下时间维度：

- 入库时间
- 采购时间
- 出库时间
- 上报发货日期

规则如下：

- 一次查询只选择一个时间维度
- 用户先确定“按哪个维度查”
- 再选择对应时间范围

---

#### 三、金额口径规则
金额口径跟查询维度走。

#### 1. 按入库时间查
显示：

- 入库数量
- 入库金额

这里的金额按入库时对应金额口径统计。

#### 2. 按采购时间查
显示：

- 采购数量
- 采购金额

#### 3. 按出库时间查
显示：

- 出库数量
- 出库金额

这里的金额按出库页面填写和确认的价格口径统计。

#### 4. 按上报发货日期查
显示：

- 上报数量
- 上报金额

这里的金额按上报页面最终确认后的价格口径统计。

---

#### 四、查询结果展示逻辑
无论按哪个时间维度查询，页面都要根据当前查询结果展示商品维度汇总结果。

至少包括：

- 商品数量
- 商品金额

也就是说：

- 查的是某个业务时间维度
- 展示的是当前条件下的结果汇总

---

#### 五、按上报时间查询的特殊规则
按“上报发货日期”查询时，除了普通商品维度汇总外，还需要支持按**上报次数**统计。

因为上报行为不是一次性的，而是会发生很多次。

例如：
- 1 月 1 日到 4 月 1 日
- 该区间内可能上报了 10 次
- 也可能上报了 20 次

因此查询结果要能体现：

- 这个周期内一共上报了多少次
- 每一次上报的数量
- 每一次上报的金额
- 最后汇总总数量和总金额

---

#### 六、上报单号规则
每一次上报的上报单号，定义为：

**该次上报发货日期范围中的最后一天**

例如：
- 某次上报发货日期范围最后一天为 `2026-04-06`
- 则该次上报单号默认为 `2026-04-06`

#### 重复处理规则
若同一天存在多次上报，则在该日期后追加序号区分，例如：

- 2026-04-06-1
- 2026-04-06-2
- 2026-04-06-3

以此类推。

---

#### 七、按上报时间查询的结果结构
按上报发货日期查询时，建议结果结构分两层：

#### 第一层：按上报单/上报次数展示
每一次上报展示：

- 上报单号
- 上报数量
- 上报金额

#### 第二层：周期汇总
在当前查询周期下，展示：

- 所有上报次数的总数量
- 所有上报次数的总金额

---

#### 八、上报查询页面展示方式
如果在当前周期内，上报次数较多，例如：

- 10 次
- 20 次
- 或更多

则页面可支持：

- 分页
- 或滚动展示

同时在结果区域中，仍要能看到整个周期的：

- 总数量
- 总金额

---

#### 九、导出规则
全局进销查询模块支持导出查询结果。

但这里导出的不是厂商上报模板，而是：

**带商品全属性的查询结果导出表**

---

#### 十、按上报时间查询时的导出字段
当按上报发货日期查询时，导出表建议包括：

- 商品编码
- 商品中文名称
- 颜色
- 数量
- 上报价格
- 上报序列号

说明：

- 商品编码来自商品主数据
- 商品中文名称来自商品主数据
- 颜色来自商品主数据
- 数量来自上报结果
- 价格取上报价格
- 序列号取上报时实际发货的 SN

这个导出表不是上报模板表，而是：

**查询结果明细表**

---

#### 十一、视图建议
后续建议页面支持两类视图：

#### 1. 普通进销汇总视图
用于查看入库、采购、出库等维度下的汇总结果。

#### 2. 上报查询专用视图
用于查看上报时间范围内的：

- 上报次数
- 每次上报数量
- 每次上报金额
- 周期总数量
- 周期总金额

因为上报查询天然多一层“按上报次数”的结构。

---

#### 十二、导出粒度建议
建议后续补充支持两种导出粒度：

- 按上报单汇总导出
- 按商品明细导出

---

#### 十三、模块总结
全局进销查询模块用于按不同业务时间维度进行统一查询，并按当前查询条件展示商品数量、金额及相关上报统计结果。

其中：

- 入库、采购、出库查询按对应业务金额口径统计
- 上报查询除商品汇总外，还支持按上报次数展示和汇总
- 上报单号按发货日期范围最后一天定义，若重复则追加序号
- 模块支持导出带商品全属性的查询结果表


# 第10章 红冲作废管理模块

#### 红冲作废管理模块定稿版

#### 一、模块定位
红冲 / 作废管理模块用于统一管理系统中已经发生的：

- 红冲
- 作废
- 回退

该模块不是业务录入模块，而是一个：

**统一查看、统一追踪、统一处理红冲/作废结果的管理模块。**

也就是说，前面已经确定：
- 出库单可以红冲
- 上报单可以红冲
- 红冲后库存回退
- 红冲后 SN 回退
- 不允许物理删除

本模块用于将这些动作集中收口。

---

#### 二、模块作用
本模块至少要解决以下问题：

- 哪些单据已经红冲了
- 哪些单据被作废了
- 红冲的是哪一种单据
- 红冲原因是什么
- 谁操作的
- 什么时候操作的
- 回退了多少商品
- 回退了多少 SN
- 关联的原始单据是谁
- 是否联动撤销了其他单据

---

#### 三、管理对象
当前先纳入两类对象：

- 出库单红冲
- 上报单红冲

后续若有其他业务单据也需要作废，可继续扩展到本模块统一管理。

---

#### 四、页面结构建议
本模块建议继续采用表格型页面。

#### 页面分两层

#### 1. 列表页
用于查看所有红冲 / 作废记录。

#### 2. 明细页或抽屉
用于查看某一条红冲记录的详细信息。

---

#### 五、列表页主字段
建议先有以下字段：

- 多选框
- 红冲单号
- 原始单据号
- 单据类型
- 红冲状态
- 红冲时间
- 红冲原因
- 操作人
- 回退商品数量
- 回退 SN 数量
- 是否联动撤销
- 操作

---

#### 六、字段说明

#### 1. 红冲单号
每一次红冲都建议生成独立的红冲单号。

#### 2. 原始单据号
表示这条红冲记录对应的是哪张原始业务单据。

#### 3. 单据类型
当前建议支持：

- 出库单
- 上报单

#### 4. 红冲状态
用于标识当前这条红冲记录的处理状态。

#### 5. 回退商品数量
表示这次红冲总共回退了多少商品数量。

#### 6. 回退 SN 数量
表示这次红冲总共回退了多少个 SN。

#### 7. 是否联动撤销
例如：
- 上报单红冲时，是否连带撤销出库单

该字段必须明确展示。

---

#### 七、明细页建议展示
打开某一条红冲记录后，建议显示：

- 红冲单号
- 原始单据号
- 原始单据类型
- 红冲原因
- 红冲时间
- 操作人
- 回退商品明细
- 回退 SN 明细
- 是否联动撤销其他单据
- 联动撤销了哪些单据
- 原始导出文件状态
- 红冲后的当前结果

---

#### 八、状态建议
建议红冲记录先有以下状态：

- 待处理
- 红冲中
- 已红冲
- 红冲失败
- 已完成

#### 状态说明
- 待处理：已发起红冲，但还未正式完成
- 红冲中：系统正在处理库存回退、SN 回退、联动撤销
- 已红冲：红冲动作已完成
- 红冲失败：红冲过程中发生错误
- 已完成：红冲及相关联动结果全部确认完成

---

#### 九、筛选规则
列表页顶部建议支持以下筛选条件：

- 红冲单号
- 原始单据号
- 单据类型
- 红冲状态
- 红冲时间
- 操作人
- 是否联动撤销

#### 筛选方式建议
- 红冲单号：输入搜索
- 原始单据号：输入搜索
- 单据类型：下拉选择
- 红冲状态：下拉选择
- 红冲时间：时间范围选择
- 操作人：输入或下拉选择
- 是否联动撤销：是 / 否

---

#### 十、排序规则
建议支持按以下字段排序：

- 红冲时间
- 红冲单号
- 原始单据号
- 单据类型
- 红冲状态
- 回退商品数量
- 回退 SN 数量

默认排序建议：

- 按红冲时间倒序

---

#### 十一、操作规则
本模块建议以查看为主，少做修改操作。

建议支持以下操作：

- 查看详情
- 查看原始单据
- 查看关联单据
- 查看回退商品明细
- 查看回退 SN 明细
- 查看日志
- 导出当前记录

---

#### 十二、不建议直接开放的操作
本模块中不建议直接开放：

- 改库存
- 改 SN 状态
- 改红冲结果
- 删除红冲记录

因为本模块应作为：

**统一追溯和统一查看入口，不是二次修数据入口。**

---

#### 十三、联动规则展示
由于前面已经确定：

- 上报单红冲采用方案 B
- 红冲上报单时要连带撤销出库
- 库存和 SN 按原链路全部回退

因此本模块中建议明确展示：

- 是否联动撤销
- 联动撤销了哪张出库单
- 联动撤销了哪张上报单
- 最终回退结果是否成功

---

#### 十四、异常场景建议
本模块后续还需要支持识别：

- 红冲失败
- 回退数量不一致
- SN 回退不完整
- 原始单据缺失
- 联动撤销失败
- 日志不完整

---

#### 十五、补充建议
后续建议补充：

- 红冲单号生成规则
- 导出记录的字段清单
- 明细页是否支持时间线视图
- 是否支持只看异常红冲记录

---

#### 十六、模块总结
红冲 / 作废管理模块用于统一查看和追踪出库单、上报单的红冲 / 作废记录，并查看对应的商品回退、SN 回退、联动撤销和日志结果。  
本模块以列表页 + 明细页方式组织，整体以查看和追溯为主，不作为直接修数据入口。


# 第11章 权限账号管理模块

#### 权限账号管理模块定稿版

#### 一、模块定位
权限 / 账号管理模块用于统一管理系统中的账号、角色和权限分配。  
当前版本不做复杂的自定义角色体系，先固定为 3 个权限等级，用于满足当前业务管理需要。

本模块主要解决：

- 谁能登录系统
- 谁能查看哪些模块
- 谁能执行哪些操作
- 谁能导出
- 谁能红冲
- 谁能修改基础配置
- 谁能管理其他账号

---

#### 二、权限等级
当前系统权限固定分为 3 类：

- 商务
- 操作员
- 管理员

---

#### 三、角色说明

#### 1. 商务
商务角色更偏向查看、统计、导出和业务跟踪。

建议职责包括：

- 查看商品信息
- 查看库存信息
- 查看全局进销查询
- 查看上报结果
- 查看 SN 全局查询
- 导出查询结果和统计结果

不建议开放：

- 新增采购
- 确认出库
- 生成上报
- 红冲 / 作废
- 基础资料配置
- 权限管理

---

#### 2. 操作员
操作员角色更偏向日常业务操作。

建议职责包括：

- 采购入库
- SN 上传与补录
- 库存查询
- 商品出库
- 序列号上报
- 全局进销查询
- SN 全局查询
- 导出业务表格

不建议开放：

- 红冲最终确认
- 基础资料配置
- 权限管理

---

#### 3. 管理员
管理员角色拥有系统最高权限。

建议职责包括：

- 全部查看权限
- 全部操作权限
- 红冲 / 作废权限
- 基础资料配置权限
- 经销商固定信息修改确认权限
- 权限管理权限
- 日志查看权限

---

#### 四、管理对象
本模块当前建议管理以下对象：

- 账号
- 角色
- 账号状态
- 角色与账号绑定关系

---

#### 五、页面结构建议
本模块建议采用两页结构：

#### 1. 账号管理页
用于管理系统登录账号。

#### 2. 角色权限说明页
用于查看三种固定角色的权限范围。

当前版本不建议开放自由创建角色，只做固定角色选择和绑定。

---

#### 六、账号管理页字段建议
建议字段包括：

- 账号名称
- 登录账号
- 角色
- 状态
- 创建时间
- 最后登录时间
- 备注
- 操作

操作包括：

- 新增账号
- 编辑账号
- 启用
- 停用
- 重置密码
- 查看详情

---

#### 七、字段说明

#### 1. 账号名称
用于识别账号所属人员。

#### 2. 登录账号
用于系统登录，必须唯一。

#### 3. 角色
固定从以下三类中选择：

- 商务
- 操作员
- 管理员

#### 4. 状态
建议统一为：

- 启用
- 停用

#### 5. 创建时间
记录账号建立时间。

#### 6. 最后登录时间
记录最近一次登录时间。

#### 7. 备注
用于补充说明。

---

#### 八、账号状态规则
建议账号状态只有两种：

- 启用
- 停用

#### 状态说明
- 启用：可正常登录并使用系统
- 停用：不可登录系统，但历史操作记录保留

---

#### 九、角色权限建议矩阵

#### 1. 商务
建议开放：

- 商品管理：查看
- 采购入库：查看
- 供应商管理：查看
- 库存查询：查看
- 商品出库：查看
- 序列号上报：查看
- 全局进销查询：查看、导出
- SN 全局查询：查看
- 红冲作废管理：查看
- 基础资料配置：不可操作
- 权限账号管理：不可操作

---

#### 2. 操作员
建议开放：

- 商品管理：查看
- 采购入库：新增、编辑、SN 上传、补录
- 供应商管理：查看
- 库存查询：查看、导出、补录 SN
- 商品出库：新增、编辑、确认出库
- 序列号上报：新增、编辑、生成上报表
- 全局进销查询：查看、导出
- SN 全局查询：查看
- 红冲作废管理：查看
- 基础资料配置：不可操作
- 权限账号管理：不可操作

---

#### 3. 管理员
建议开放：

- 所有模块的查看权限
- 所有业务模块的操作权限
- 红冲 / 作废权限
- 基础资料配置权限
- 经销商固定信息确认修改权限
- 权限账号管理权限
- 日志查看权限

---

#### 十、操作规则

#### 1. 新增账号
- 只能由管理员新增
- 登录账号必须唯一
- 必须选择角色
- 默认状态建议为启用

#### 2. 编辑账号
- 只能由管理员编辑
- 可修改账号名称、角色、状态、备注
- 登录账号建议限制修改

#### 3. 停用账号
- 停用后不可登录
- 历史操作记录继续保留

#### 4. 重置密码
- 只能由管理员操作
- 建议记录重置时间和操作人

---

#### 十一、删除规则
建议当前版本不开放物理删除账号。

规则如下：

- 账号如不再使用，只允许停用
- 不允许删除已有历史操作记录的账号

这样可以保证日志追溯完整。

---

#### 十二、筛选规则
账号管理页建议支持以下筛选条件：

- 账号名称
- 登录账号
- 角色
- 状态

---

#### 十三、排序规则
建议支持按以下字段排序：

- 账号名称
- 登录账号
- 角色
- 状态
- 创建时间
- 最后登录时间

默认排序建议：

- 按创建时间倒序

---

#### 十四、补充建议

#### 1. 建议保留完整操作日志
权限和账号相关操作建议记录：

- 新增账号
- 编辑账号
- 停用账号
- 启用账号
- 重置密码
- 角色变更

#### 2. 建议管理员不能被物理删除
管理员账号建议只允许停用，不允许删除。

#### 3. 建议后续再扩展数据权限
当前版本先不细分数据范围权限。  
后续如果需要，再扩展例如：

- 只能看某些供应商
- 只能看某些商品
- 只能看某些上报记录

---

#### 十五、模块总结
权限 / 账号管理模块用于统一管理系统账号和固定角色权限。  
当前版本采用三层固定权限体系：

- 商务
- 操作员
- 管理员

通过固定角色与账号绑定，实现系统访问控制、操作控制和管理控制。
