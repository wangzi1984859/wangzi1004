# SN ERP UI 设计基线 V1.1（修正增强版）

> 基于 V1.0 基础上叠加修正点
> 叠加来源：《SN ERP 启动方案 V1.1 修正收口版》修正点 #4 / #8
> 版本：V1.1
> 日期：2026-04-26

---

## 一、V1.0 → V1.1 变更说明

V1.1 在 V1.0 基础上新增以下内容：

| 序号 | 变更来源 | 变更内容 |
|------|---------|---------|
| 1 | 修正点 #4 | 确认入库按钮与保存草稿按钮视觉区分规范 |
| 2 | 修正点 #8 | 轻量 Dashboard 视觉规范（替代 V1.0 暂缓方案） |
| 3 | V6 对齐 | 30+ 状态标签枚举映射表完整版（补充 V6 全部状态字典） |
| 4 | 修正点 #4 | 权限感知按钮规范（按角色显示/隐藏/禁用按钮） |
| 5 | 修正点 #4 | 确认入库操作日志 UI 表现规范 |

---

## 二、确认入库按钮视觉区分规范（修正点 #4）

### 2.1 按钮层级定义

| 按钮类型 | 样式 | 使用场景 | 示例 |
|---------|------|---------|------|
| 主操作按钮 | 主色实心 #2563EB 白色文字 | 页面最重要的操作 | 确认入库、新建采购单 |
| 次操作按钮 | 白色底色主色边框主色文字 | 辅助操作 | 保存草稿 |
| 危险按钮 | 红色实心 #EF4444 白色文字 | 不可逆操作 | 红冲、删除 |
| 普通按钮 | 白色底色灰色边框灰色文字 | 一般操作 | 取消、重置 |
| 文字按钮 | 无边框无背景主色文字 | 表格操作列 | 编辑、查看 |

### 2.2 确认入库与保存草稿区分

在采购详情页（草稿状态）：

| 按钮 | 样式 | 位置 | 大小 |
|------|------|------|------|
| 保存草稿 | 次操作按钮（白底主色边框） | 页面标题右侧靠左 | 高度 32px |
| 确认入库 | 主操作按钮（主色实心） | 页面标题右侧靠右（更显眼位置） | 高度 36px，比保存草稿略大 |

视觉区分要点：
- 确认入库使用实心主色按钮，视觉权重最高
- 保存草稿使用边框按钮，视觉权重低于确认入库
- 确认入库按钮比保存草稿按钮高 4px
- 确认入库按钮左侧可加图标（如 ✓ 图标）
- 两个按钮之间间距 12px

### 2.3 权限感知按钮规范

| 当前用户角色 | 保存草稿 | 确认入库 | 红冲 |
|-------------|---------|---------|------|
| 管理员 | 显示 | 显示 | 显示 |
| 操作员（有确认权限） | 显示 | 显示 | 隐藏 |
| 操作员（无确认权限） | 显示 | 不显示或禁用灰化 | 隐藏 |
| 商务 | 全部隐藏 | 全部隐藏 | 全部隐藏 |

按钮禁用态样式：
- 背景：#F3F4F6
- 文字：#94A3B8
- 鼠标：not-allowed
- Tooltip：悬浮提示"您没有此操作权限"

### 2.4 确认入库弹窗规范

点击"确认入库"按钮后弹出 L2 确认弹窗：

```
┌─────────────────────────────────┐
│  确认入库                        │
│─────────────────────────────────│
│                                 │
│  即将确认入库以下采购单：         │
│                                 │
│  采购单号：CG202604260001       │
│  供应商：XXX 供应商              │
│  商品数量：5 种                  │
│  总数量：500 件                  │
│  已录入 SN：420 条               │
│  待补录 SN：80 条                │
│                                 │
│  ⚠ 确认入库后不可撤销草稿编辑    │
│                                 │
│              [取消]  [确认入库]  │
│                       红色按钮   │
└─────────────────────────────────┘
```

- 确认按钮使用红色 #EF4444（L2 级别）
- 弹窗内展示采购单关键信息摘要
- 底部警告文案提醒不可逆

### 2.5 确认入库后 UI 状态变化

确认入库成功后：
- 采购单状态标签从"草稿"变为"已入库"
- "保存草稿"按钮消失
- "确认入库"按钮消失
- 出现"查看详情"按钮
- 页面顶部弹出成功 Toast："采购单 CG202604260001 确认入库成功"
- 如有管理后台日志需求，操作日志自动记录

---

## 三、轻量 Dashboard 视觉规范（修正点 #8）

### 3.1 第一阶段 Dashboard 范围

第一阶段只做轻量 Dashboard，包含：
- 欢迎区
- 快捷入口
- 简单统计卡片
- 待处理事项占位

复杂图表、趋势分析、深度统计看板放到第二阶段。

### 3.2 欢迎区

```
┌──────────────────────────────────────────────┐
│  早上好，张三                                  │
│  今天是 2026 年 4 月 26 日 星期日               │
└──────────────────────────────────────────────┘
```

- 标题：20px，字重 600，颜色 #1E293B
- 副标题：14px，字重 400，颜色 #64748B
- 根据时间段显示：早上好 / 下午好 / 晚上好

### 3.3 统计卡片区

```
┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐
│ 商品总数  │ │ 库存商品  │ │ SN 总数   │ │ 采购单数  │
│   128    │ │   86     │ │  12,450  │ │   45     │
│ 较昨日 +3 │ │ 较昨日 +0 │ │ 较昨日+120│ │ 较昨日 +2 │
└──────────┘ └──────────┘ └──────────┘ └──────────┘
```

| 规范项 | 要求 |
|--------|------|
| 布局 | 4 列等宽网格 |
| 卡片宽度 | 自适应，间距 16px |
| 卡片圆角 | 8px |
| 卡片阴影 | --shadow-sm |
| 卡片背景 | #FFFFFF |
| 数字字号 | 28px，字重 600，颜色 #1E293B |
| 标题字号 | 13px，字重 400，颜色 #64748B |
| 趋势文字 | 12px，颜色 #10B981（正增长）/ #EF4444（负增长） |
| 数据来源 | 简单 COUNT 查询，不做复杂计算 |

### 3.4 快捷入口

```
┌──────────────────────────────────────────────┐
│  快捷操作                                      │
│                                                │
│  [新建采购单]  [库存查询]  [SN 上传]  [商品管理] │
│                                                │
└──────────────────────────────────────────────┘
```

| 规范项 | 要求 |
|--------|------|
| 标题 | 16px，字重 600 |
| 按钮 | 次操作按钮样式（白底主色边框） |
| 图标 | 每个按钮上方 24px 图标 |
| 布局 | 4 列等宽网格 |
| 权限 | 按当前用户角色过滤，商务不显示"新建采购单" |

### 3.5 待处理事项

```
┌──────────────────────────────────────────────┐
│  待处理事项                          查看全部 > │
│                                                │
│  ● 待确认采购单    3 单                         │
│  ● 待补录 SN      86 条                        │
│  ● 待处理红冲     0 条                         │
│                                                │
└──────────────────────────────────────────────┘
```

| 规范项 | 要求 |
|--------|------|
| 标题 | 16px，字重 600 |
| 列表项 | 左侧圆点（主色）+ 文字 + 数量 |
| 数量 | 14px，字重 600，主色 |
| 点击 | 点击跳转对应页面（带筛选条件） |
| 空状态 | "暂无待处理事项" + 空状态图标 |

### 3.6 Dashboard 页面整体布局

```
┌──────────────────────────────────────────────┐
│  欢迎区（通栏）                                 │
├──────┬──────┬──────┬──────┤
│ 统计卡片1 │ 统计卡片2 │ 统计卡片3 │ 统计卡片4 │
├──────┴──────┴──────┴──────┤
│  快捷操作（通栏）                              │
├──────────────────────────────────────────────┤
│  待处理事项（通栏）                             │
└──────────────────────────────────────────────┘
```

### 3.7 暂缓范围说明

以下内容放入第二阶段，不在第一阶段实现：
- 趋势折线图（采购/出库/上报趋势）
- 库存金额分布饼图
- 供应商维度统计柱状图
- SN 状态分布环形图
- 日报/周报/月报自动统计
- 数据导出快捷入口（Dashboard 上的导出按钮）
- 自定义看板布局（拖拽排列卡片）

---

## 四、30+ 状态标签枚举映射完整版（V6 对齐）

### 4.1 说明

V1.0 已列出 30+ 状态标签映射。V1.1 补充以下对齐 V6 状态字典的完整映射，确保所有模块状态均有对应 UI 表现。

### 4.2 补充状态映射

#### 采购 SN 上传方式标签

| 枚举 | 中文文案 | 背景色 | 文字色 |
|------|---------|--------|--------|
| EXCEL | Excel 导入 | #D1FAE5 | #047857 |
| TXT | TXT 导入 | #DBEAFE | #1D4ED8 |
| CSV | CSV 导入 | #E0E7FF | #4338CA |
| IMAGE | 图片识别 | #FEF3C7 | #92400E |
| MANUAL | 手工录入 | #F3F4F6 | #6B7280 |

#### 价格类型标签

| 枚举 | 中文文案 | 背景色 | 文字色 |
|------|---------|--------|--------|
| ORIGINAL | 原价 | #DBEAFE | #1D4ED8 |
| PROMO | 特价 | #FEF3C7 | #92400E |
| FIXED | 一口价 | #E0E7FF | #4338CA |
| ANY | 不限 | #F3F4F6 | #6B7280 |

#### 经销商信息状态标签

| 枚举 | 中文文案 | 背景色 | 文字色 |
|------|---------|--------|--------|
| DEFAULT | 默认值 | #D1FAE5 | #047857 |
| MODIFIED_PENDING | 已修改待确认 | #FEF3C7 | #92400E |
| CONFIRMED | 已确认修改 | #DBEAFE | #1D4ED8 |

#### 文件导出类型标签

| 枚举 | 中文文案 | 背景色 | 文字色 |
|------|---------|--------|--------|
| PREVIEW | 预览表 | #DBEAFE | #1D4ED8 |
| FCS_EXPORT | 正式导出 | #D1FAE5 | #047857 |
| QUERY_EXPORT | 查询导出 | #E0E7FF | #4338CA |

#### SN 异常标签（abnormal_flags）

| 枚举 | 中文文案 | 背景色 | 文字色 |
|------|---------|--------|--------|
| TRACE_BROKEN | 链路断裂 | #FEE2E2 | #B91C1C |
| DUPLICATE | 重复记录 | #FEE2E2 | #B91C1C |
| OWNERSHIP_MISMATCH | 归属不一致 | #FEE2E2 | #B91C1C |
| ROLLBACK_FAILED | 回退失败 | #FEE2E2 | #B91C1C |
| STATUS_CONFLICT | 状态冲突 | #FEE2E2 | #B91C1C |

### 4.3 状态标签总数统计

| 模块 | 状态数量 |
|------|---------|
| 采购入库单 | 3 |
| 出库单 | 5 |
| 上报单 | 5 |
| 红冲记录 | 5 |
| SN 主状态 | 5 |
| 库存状态 | 4 |
| SN 补录状态 | 3 |
| SN 提取状态 | 5 |
| SN 提取结果 | 4 |
| 上报分配状态 | 5 |
| 商品/供应商状态 | 2 |
| 账号状态 | 2 |
| 角色标签 | 3 |
| SN 上传方式 | 5 |
| 价格类型 | 4 |
| 经销商信息状态 | 3 |
| 文件导出类型 | 3 |
| SN 异常标签 | 5 |
| **合计** | **67** |

---

## 五、权限感知 UI 规范补充

### 5.1 按钮权限控制原则

| 原则 | 说明 |
|------|------|
| 前端隐藏/禁用 | 无权限按钮不显示或禁用灰化 |
| 后端校验 | 前端隐藏不等于权限控制，后端必须独立校验权限 |
| 禁用优于隐藏 | 关键操作建议禁用 + Tooltip 提示，而非直接隐藏 |
| 商务只读 | 商务角色所有编辑/新增/删除/确认按钮全部隐藏 |

### 5.2 关键操作按钮权限矩阵

| 操作 | 管理员 | 操作员（有确认权限） | 操作员（无确认权限） | 商务 |
|------|--------|-------------------|-------------------|------|
| 保存草稿 | 显示 | 显示 | 显示 | 隐藏 |
| 确认入库 | 显示 | 显示 | 禁用+提示 | 隐藏 |
| 红冲 | 显示 | 隐藏 | 隐藏 | 隐藏 |
| 修改价格 | 按开关 | 按开关 | 隐藏 | 隐藏 |
| 导出 | 显示 | 显示 | 显示 | 显示 |

---

## 六、操作日志 UI 表现规范

### 6.1 确认入库日志展示

确认入库操作日志在操作日志页面展示：

| 字段 | 展示方式 |
|------|---------|
| 操作人 | 用户名 |
| 操作时间 | YYYY-MM-DD HH:mm:ss |
| 操作模块 | "采购入库" |
| 操作动作 | "确认入库" |
| 目标单号 | 采购单号（可点击跳转） |
| 操作前状态 | StatusTag "草稿" |
| 操作后状态 | StatusTag "已入库" |
| 入库商品数量 | 数字 |
| 已录入 SN 数量 | 数字 |
| 待补录 SN 数量 | 数字（>0 时标黄） |

### 6.2 日志详情弹窗

点击日志行展开详情弹窗，JSONB 详情字段格式化展示：

```
┌─────────────────────────────────────┐
│  操作日志详情                         │
│─────────────────────────────────────│
│  操作人：张三（管理员）                │
│  操作时间：2026-04-26 14:30:00       │
│  采购单号：CG202604260001            │
│  状态变更：草稿 → 已入库              │
│                                     │
│  入库明细：                           │
│  ┌────────┬──────┬─────┬─────┐     │
│  │ 商品    │ 数量  │已录入SN│待补录│     │
│  ├────────┼──────┼─────┼─────┤     │
│  │ 商品A   │ 100  │  80  │  20 │     │
│  │ 商品B   │  50  │  50  │   0 │     │
│  └────────┴──────┴─────┴─────┘     │
│                                     │
│                              [关闭]  │
└─────────────────────────────────────┘
```

---

## 七、V1.0 原有内容继承说明

V1.1 不重复 V1.0 已有内容。以下章节直接继承 V1.0，无变更：

| V1.0 章节 | V1.1 状态 |
|-----------|----------|
| 一、设计原则 | 继承不变 |
| 二、色彩体系 | 继承不变 |
| 三、字体体系 | 继承不变 |
| 四、间距体系 | 继承不变 |
| 五、圆角体系 | 继承不变 |
| 六、阴影体系 | 继承不变 |
| 七、布局规范 | 继承不变 |
| 八、核心组件规范 | 继承不变 |
| 九、三级危险操作确认规范 | 继承不变 |
| 十、10 条设计禁区红线 | 继承不变 |
| 十一、7 个页面级视觉标准 | Dashboard 章节替换为本文件第三章 |
| 十二、34 条 UI 验收标准 | 继承不变 |
| 十四、前端实现要求 | 继承不变 |

---

**以上为 SN ERP UI 设计基线 V1.1（修正增强版）。与 V1.0 配合使用。**
