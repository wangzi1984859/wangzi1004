# 账号级授权机制记录

> 基于：V1.2 规则 "管理员可单独授予某个操作员 purchase:confirm 确认入库权限"
> 创建日期：2026-04-29
> 版本：1.1（第 4-R 收口）

---

## 一、为什么需要 sys_user_permission

当前权限来自角色-权限绑定（`sys_role_permission`），operator 角色默认不能拥有 `purchase:confirm`。V1.2 要求管理员可给某个操作员单独授予确认入库权限，但不能把该权限加回 operator 角色。

因此新增 `sys_user_permission` 表，用于向单个用户授予角色权限之外的额外权限，实现账号级授权覆盖。

---

## 二、sys_user_permission 表结构

| 字段 | 类型 | 说明 |
|------|------|------|
| id | UUID PK | `gen_random_uuid()` |
| user_id | UUID FK → sys_user.id | 被授权用户 |
| permission_id | UUID FK → sys_permission.id | 授予的权限 |
| effect | VARCHAR(10) DEFAULT 'ALLOW' | ALLOW / DENY（DENY 当前预留） |
| created_by | UUID FK → sys_user.id | 授权人 |
| created_at | TIMESTAMPTZ | 授权时间 |
| remark | VARCHAR(500) | 备注 |

唯一约束：`UNIQUE(user_id, permission_id)`

---

## 三、权限计算规则

```
用户有效权限 = 角色权限 ∪ 用户级 ALLOW 权限（去重）
```

- JwtStrategy.validate() 加载角色权限 + 用户 ALLOW 权限
- AuthService.login() 合并后返回
- PermissionsGuard 对合并后的 permissions 数组校验

**admin 不硬编码超级权限**，仍按数据库权限 + 用户级权限计算。

---

## 四、角色权限不变

| 角色 | 默认权限数 | 含 purchase:confirm |
|------|-----------|---------------------|
| admin | 21 | ✅（来自角色） |
| operator | 10 | ❌（默认无，需单独授权） |
| business | 8 | ❌（不可授予） |

operator 角色默认不拥有 `purchase:confirm`，不拥有 `inventory:manage`。

---

## 五、授权/撤销接口

### 授权

```
POST /api/auth/users/:userId/permissions
Body: { "permissionCode": "purchase:confirm", "remark": "..." }
Permission: system:permission:manage
```

限制：
- 只能授予 `purchase:confirm`
- 只能授予 operator 角色用户
- 不能授予 business
- 幂等（同一用户+权限 upsert）

### 撤销

```
DELETE /api/auth/users/:userId/permissions/:permissionCode
Permission: system:permission:manage
```

限制（与授权接口同等边界）：
- 只能撤销 operator 角色用户的 `purchase:confirm` 授权
- 不能撤销 business / admin 的授权
- 不能撤销非 purchase:confirm 的授权
- 目标用户必须存在且启用

---

## 六、effect 字段数据库约束

`effect` 字段在数据库层有 CHECK 约束：

```sql
CONSTRAINT "ck_sys_user_permission_effect" CHECK ("effect" IN ('ALLOW', 'DENY'))
```

- 当前权限计算只处理 `effect = 'ALLOW'`
- `DENY` 仅预留，不参与当前权限计算
- 后续如果扩展更多账号级权限，必须重新评审接口边界

---

## 七、新 migration

路径：`packages/server/prisma/migrations/20260428155327_add_user_permission/migration.sql`

- CREATE TABLE `sys_user_permission`
- 3 FK → sys_user, sys_permission, sys_user
- UNIQUE (user_id, permission_id)
- 未修改 init migration

---

## 变更记录

| 版本 | 日期 | 变更内容 | 负责人 |
|------|------|---------|--------|
| 1.0 | 2026-04-29 | 初始创建：sys_user_permission 表 + 账号级授权机制 | backend |
| 1.1 | 2026-04-29 | 第 4-R：收紧撤销接口边界 + effect CHECK 约束 | backend |
